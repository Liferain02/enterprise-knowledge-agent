# 实验室科研助手：知识演化与评测驱动优化任务书

> 项目：`Liferain02/enterprise-knowledge-agent`
>
> 参考项目：`dianayyds/research_copilot`
>
> 日期：2026-09-03
>
> 当前阶段：主体架构已基本完成，进入“知识生命周期闭环 + 真实评测收口”阶段。
>
> 核心原则：
>
> 1. 不为展示技术栈继续增加 Agent、Supervisor、图数据库、向量数据库或中间件。
> 2. 不把 RAG 局限为“检索后回答”，而是向“知识获取 → 研究 → 验证 → 沉淀 → 再利用”的科研知识生命周期演化。
> 3. 所有新能力必须有明确数据来源、可信门禁、ACL、可追溯 Provenance 与可量化评测。
> 4. 评测结果不做包装：Retrieval 指标、Agent 指标、模型 Judge、人工校准必须分别说明，不能混为“准确率”。

---

# 1. 背景：来自外部评论的启发

需要保留并正面回应下面这条评论：

> “RAG也不是完全不能写，结合具体的memory和knowledge去讨论自进化，去做知识自沉淀和结构化入库，去聊wiki和graph结构，去分析召回率和稳定性，这就是一个非常不错的agent模块开发项目，而不是局限于一个单纯的问答。”

这条评论的核心不是“给 RAG 再加几个组件”，而是要求项目从：

```text
文档
→ 检索
→ LLM
→ 回答
```

升级为：

```text
原始资料
→ Retrieval
→ Research
→ Verified Claim
→ Memory / Knowledge
→ Structured Knowledge
→ Wiki / Relationship
→ 再进入 Retrieval
```

也就是说，项目的核心价值不再是“问答”，而是：

> **科研知识生命周期管理。**

---

# 2. 对这条评论的具体分析

## 2.1 Memory：不能只是聊天记忆

低质量做法：

```text
用户对话
→ LLM 自动总结
→ 写入长期记忆
```

问题是：

- 模型错误会永久沉淀；
- 无法解释记忆来自哪里；
- 权限变化后可能继续泄露；
- recommendation / inference 可能被错误当成 fact；
- 长期记忆会形成“模型自己引用自己”的闭环。

当前项目已经形成更可信的 Memory 基线：

```text
Research Run
→ Claim
→ Reviewer
→ Evidence
→ 用户确认
→ Mem0 infer=False
→ Recall 时重新 ACL 验证
```

因此下一步不应再加 Memory Agent，也不需要第二套向量记忆。

真正需要做的是：

> 从 Evidence-backed Memory 进一步演化为 Evidence-backed Knowledge。

---

## 2.2 Knowledge：当前最大的增量空间

当前知识库主要来自：

```text
PDF / DOCX / MD / TXT
→ Chunk
→ Vector / BM25
→ Retrieval
```

这是“原始资料入库”。

下一阶段应该增加：

```text
Research Run
→ Reviewer PASS Claim
→ 用户/项目负责人发布
→ Structured Knowledge Record
→ 再进入后续 Retrieval
```

这才真正形成：

> **知识自沉淀 / Knowledge Promotion。**

关键区别：

```text
Mem0 = 我个人以后还想记住什么
Knowledge = 项目正式认可并可被后续检索引用什么
```

二者不能混为一套语义。

---

## 2.3 Wiki：值得做，但应该是结构化知识的视图

不应该：

```text
LLM 自动生成一篇 Wiki 文档
→ 存全文
```

更合理的是：

```text
Structured Knowledge Records
          ↓
       Wiki View
```

例如：

```text
Project
├── Verified Facts
├── Decisions
├── Progress
├── Open Questions
├── Experiments
└── Research Runs / Evidence
```

每一项都应该能够追溯：

```text
Knowledge Record
→ Claim
→ Research Run
→ Evidence
→ Raw Document
```

Wiki 是展示层，不是第二套知识源。

---

## 2.4 Graph：先做关系模型，不急着上图数据库

“聊 Graph”不等于必须引 Neo4j。

第一阶段只需要支持关系语义：

```text
supports
contradicts
supersedes
derived_from
uses_config
```

例如：

```text
Experiment E12
→ supports
→ Fact F8

Fact F8
→ supersedes
→ Fact F3
```

初期普通数据库表即可。

只有当真实查询出现：

```text
查 3 跳以内哪些实验、论文和配置共同支持某个结论
```

再评估 Knowledge Graph / Neo4j。

---

## 2.5 自进化：必须改成“受控知识演化”

不建议在 README / 简历里泛化写：

> “系统能够自进化。”

更准确的工程表述应该是：

> **Verified Knowledge Evolution / 受控知识演化**

流程：

```text
Raw Evidence
→ Research
→ Claim
→ Reviewer
→ Human / Project Approval
→ Structured Knowledge
→ Future Retrieval
```

其中必须限制：

- LLM 不可自行发布事实；
- 必须保留 Evidence；
- 必须保留 Provenance；
- 必须有 ACL；
- 必须支持撤回 / supersede；
- 不能依赖 Derived Knowledge 自我证明。

---

# 3. 当前项目已经完成到什么程度

## 3.1 Retrieval / Normal RAG

已经具备：

```text
Vector Retrieval
+
BM25
+
RRF
+
Rerank
+
ACL
```

并有：

- Query Expansion；
- Standalone Query Rewrite；
- 多轮真实路由；
- Source Cards；
- SSE；
- Retrieval Ablation / Multi-turn Eval。

Standalone 已有正式消融结果：

- 数据集：24 条；
- Trigger Precision / Recall / F1：1.000 / 1.000 / 1.000；
- 正例 Hit@5：0.867 → 1.000；
- 正例 MRR@5：0.867 → 0.933；
- ACL violation：0；
- Positive Regression Rate：0；
- 单请求最多 2 queries；
- 平均额外延迟约 525ms。

结论：

> Standalone 已经完成，不再作为开发重点。

## 3.2 Deep Research

当前固定架构：

```text
Researcher
→ Analyst
→ Reviewer
→ ≤1 Revision
→ Generation
```

特点：

- 不使用动态 Supervisor；
- 不增加自由 Agent 通信；
- Evidence / Claim / Review 均结构化；
- Reviewer 最多触发一次 Revision；
- Deep Research 由用户显式开启，不默认替代 Normal RAG。

## 3.3 Research Run / Trace

已经实现：

```text
EvidencePackage
AnalysisReport
ReviewReport
Trace
Metrics
Final Answer
Source Cards
```

并支持：

- SQLite 持久化；
- API；
- 前端展开；
- Research Run ACL；
- 历史权限变化后 fail closed。

## 3.4 Evidence-backed Memory

已经实现：

```text
Reviewer PASS
+
claim_type = fact
+
有效 Evidence
+
用户确认
→ Mem0 infer=False
```

Memory metadata 包含：

```text
memory_type
scope
project_id
research_run_id
claim_id
source_ids
review_decision
user_confirmed
verified
```

召回流程：

```text
Mem0 Search
→ Recall Gate
→ Research Run
→ Claim
→ Source
→ Current ACL
→ Allow / Drop
```

---

# 4. 参考项目真正值得借鉴的地方

参考项目 `research_copilot` 并不是简单 Multi-Agent 项目。

其默认主链更准确是：

```text
Planner
→ Plan Tasks
→ per-node ReAct
→ MCP Action
→ Observation
→ Final Synthesis
```

它还有：

```text
Agent
→ Tool
→ Observation
→ Tool
→ ...
→ Final Answer
```

这种单 Agent Tool Loop。

因此参考项目不属于：

```text
Researcher
→ Analyst
→ Reviewer
```

这种角色分工型 Multi-Agent Team。

## 4.1 对比两者 Agent 设计

### research_copilot

```text
DeepSeek Planner
→ 结构化 tasks
→ 每个 task 独立 ReAct
→ MCP / Tool
→ Observation
→ Final Answer
```

优势：

- Tool orchestration 灵活；
- 支持动态任务拆解；
- 对 GitHub / Web / Memory / TODO 等 Tool 较友好；
- Execution Trace 完整。

缺点：

- 多轮 Tool Loop 成本较高；
- 角色职责没有严格分开；
- 默认主链不天然形成 Claim-level Review；
- 不适合直接作为你当前科研可信问答的替代方案。

### 当前项目

```text
Researcher
→ Evidence

Analyst
→ Claim

Reviewer
→ Evidence-backed Review

Revision
→ ≤ 1
```

优势：

- 角色职责可解释；
- Claim 和 Evidence 可以一一追踪；
- Reviewer 能做结构化可信门禁；
- 更适合做 Claim-level Evaluation；
- 更适合科研知识沉淀。

结论：

> **不修改当前 Multi-Agent 主架构，不改成 Plan-ReAct-MCP。**

参考项目应借鉴 Tool / Memory / Knowledge 生命周期，而不是照搬 Agent 主链。

---

# 5. 参考项目是怎么评测的

## 5.1 最完整的是 Retrieval / Query Rewrite Benchmark

参考项目构造了一套小型语义通信 benchmark：

- 15 个固定问题；
- 多组结构化语义通信材料；
- 每条 gold evidence 带唯一标记；
- 加入 Semantic Web、普通 ASR、Shannon 等 distractor；
- 对部分问题增加 paraphrase。

对比：

```text
baseline
step_back
hyde
hybrid_3
hybrid_full
```

主要指标：

```text
Hit@1
Hit@3
Hit@5
MRR@5
Coverage@5
Average Gold Rank
Rank StdDev
Drift Rate
```

README 中报告：

```text
Hit@1：0.80 → 0.93
MRR@5：0.869 → 0.967
Coverage@5：100%
Top1 Drift：13.3% → 6.7%
```

但是这些是：

> **Retrieval / Query Rewrite 指标，不是最终回答准确率。**

并且其 benchmark 测试环境显式使用：

```text
VECTOR_STORE_PROVIDER = stub
EMBEDDING_PROVIDER = stub
LLM_PROVIDER = stub
```

所以更准确的描述是：

> 受控检索策略 benchmark。

不能把 93.3% 解释为最终答案准确率。

## 5.2 Agent 主链主要做工程行为测试

主要验证：

- 默认是否进入 Plan-ReAct；
- Plan / Trace 是否存在；
- Tool 是否被正确调用；
- Observation 是否形成 Evidence；
- Citation 是否生成；
- Side-effect Tool 是否被 allowlist 拦截；
- Stream Event 是否完整；
- 无 LLM Key 是否明确失败。

它没有像当前项目一样做：

```text
Normal
vs
Deep Gated
vs
Deep Full
```

的 Claim-level End-to-End 因果评测。

## 5.3 Tool-use Alignment

参考项目还有：

```text
Qwen3 0.6B
→ LoRA SFT
→ DPO
```

指标：

```text
JSON Valid Rate
Action Accuracy
Argument Match Rate
Tool-needed F1
Over-tool Rate
```

但 dry-run 明确属于 simulated metrics，不能当真实模型结果。

---

# 6. 当前项目自己的 V3 评测怎么理解

当前 V3：

| Variant | Claim Precision | Claim Recall | Claim F1 | Faithfulness | P50 |
|---|---:|---:|---:|---:|---:|
| Normal | 0.417 | 0.651 | **0.491** | 0.760 | 8.3s |
| Deep Gated | **0.433** | 0.662 | 0.472 | 0.737 | 35.7s |
| Deep Full | 0.360 | **0.734** | 0.466 | **0.781** | 42.1s |

这些数字不能简单解读为：

> Deep Research “只有 46% 正确率”。

Claim F1 是：

```text
Generated Claims
vs
Gold Claims
```

的结构化匹配指标，不是答案正确率。

## 6.1 当前 V3 真正说明的问题

### 总体

```text
Normal
→ F1 更高
→ 速度快
→ 成本低

Deep Full
→ Recall 更高
→ Faithfulness 更高
→ 成本高
```

### 按任务类别

当前观察到：

```text
实验设计
→ Deep Full 更有价值

系统实现
→ Deep Gated 有局部优势

前提判断
→ Normal 更好

证据复现
→ Deep 提升 Faithfulness，但未提升 F1
```

正确结论：

> Multi-Agent 不是全局最优，需要明确适用边界。

---

# 7. Actionability Gate 的实验价值

曾经尝试：

```text
Reviewer = REVISE
+
没有 actionable ReviewItem
→ PASS
→ 跳过 Revision
```

因果消融：

```text
Claim F1         +0.006
Faithfulness     -0.044
Latency          -14.6%
Input Token      -16.0%
Output Token     -18.5%
```

预先规定质量容差：

```text
0.02
```

Faithfulness 下降 0.044，超过门限。

因此最终：

```text
撤销 Actionability Gate
→ 恢复 Reviewer 原始 REVISE
→ 最多一次 Revision
```

这是一条重要工程结论：

> **不是所有降本优化都应该保留；评测结果反向驱动架构回退。**

---

# 8. 后续评测体系如何重新组织

后续必须拆成三层。

## 8.1 Retrieval Layer

问题：

```text
能不能找到正确证据？
```

指标：

```text
Hit@1
Hit@5
MRR@5
NDCG@5
Coverage@5
Rank StdDev
Drift Rate
Regression Rate
ACL Violation
Latency
```

适用于：

- Hybrid Retrieval；
- Standalone；
- Technical-aware Tokenizer；
- Parent-child Retrieval；
- Step-back（如后续触发）。

## 8.2 Agent / Generation Layer

问题：

```text
找到证据以后，能不能形成高质量科研回答？
```

指标：

```text
Claim Precision
Claim Recall
Claim F1
Faithfulness
Premise Handling
Citation Quality
Latency
Input Token
Output Token
Logical Calls
```

适用于：

```text
Normal
vs
Deep
vs
Selective Deep Routing
```

## 8.3 Product / Stability Layer

问题：

```text
真实用户用的时候稳不稳？
```

指标：

```text
Browser Acceptance Pass Rate
Route Correctness
Gold Source Hit
HTTP Errors
Console Errors
SSE Completeness
Research Run Persistence
Memory Recall / Revoke
ACL Downgrade
P50 / P95
```

---

# 9. 当前最重要的评测新任务：Selective Routing Evaluation

当前最大问题不是 Deep F1 不够高，而是：

> **是否可以只把真正适合的任务送给 Deep。**

新增冻结实验：

```text
A. All Normal
B. All Deep Full
C. Selective Routing
```

Selective Routing 第一版不要增加 Router Agent。

可以先基于：

```text
task category
+
complexity signal
+
explicit research_mode
```

或者确定性规则做实验。

目标：

```text
质量 ≥ Normal
Faithfulness 接近 Deep
成本 << All Deep
```

指标：

```text
Claim F1
Faithfulness
Recall
P50 / P95
Token
Calls
Deep Activation Rate
Wrong Routing Rate
```

如果没有稳定收益，就继续保持显式 Deep 模式。

---

# 10. 接下来具体任务与优先级

# P0：完成当前真实验收收尾

## P0-1 跑 8 条真实 Browser Acceptance

已有：

```text
tests/eval/run_multiturn_browser_acceptance.py
```

正式执行并固定：

```text
total
completed
route_passed
gold_source_hit
HTTP errors
console errors
P50 / P95 latency
```

## P0-2 完成一条真实 Memory E2E

完整流程：

```text
Deep Research
→ Reviewer PASS Fact
→ 用户点击确认
→ Mem0
→ 新 Session
→ Recall
→ 删除 / Revoke
→ 不再 Recall
```

再加：

```text
用户权限降级
→ Confirmed Research Fact
→ Recall Gate 拦截
```

## P0-3 人工 Calibration

有真人资源则完成：

```text
36 independent labels
```

否则必须明确：

```text
Qwen Judge Complete
Codex Model Simulation Complete
Independent Human Calibration Pending
```

---

# P1：Verified Knowledge Promotion

这是最重要的新功能。

目标：

> 把可信 Research Claim 从个人 Memory 提升为项目结构化 Knowledge。

## P1-1 新增 Knowledge Record

建议表：

```text
research_knowledge_records
```

字段：

```text
id
project_id
knowledge_type
statement
status
version
research_run_id
claim_id
source_ids_json
created_by
published_by
created_at
updated_at
supersedes_id
```

第一版只支持：

```text
fact
decision
```

状态：

```text
active
superseded
revoked
```

## P1-2 Publish Gate

两个动作必须严格分离：

```text
确认并记住
→ Personal Memory

发布为项目知识
→ Project Knowledge
```

Knowledge Promotion 必须满足：

```text
ReviewReport = PASS
Claim type allowed
Reviewer item supported = true
issue_type = none
source_ids 非空
Evidence ACL valid
Current Project Permission valid
Human Publish Action
```

## P1-3 Provenance

每条知识必须保留：

```text
Knowledge Record
→ Claim
→ Research Run
→ Evidence
→ Source Document
```

禁止只存裸文本。

## P1-4 防止 Knowledge Self-Reinforcement

区分：

```text
raw_document
derived_knowledge
external_evidence
```

如果 Derived Knowledge 再进入 Retrieval，Promotion 时至少要求：

> 存在一条最终可追溯到 raw_document 的证据链。

禁止 Derived Knowledge 无限互证。

## P1-5 Revoke / Supersede

支持：

```text
旧结论
→ superseded
→ 新结论
```

以及：

```text
错误结论
→ revoked
```

Retrieval 默认只使用：

```text
active
```

---

# P2：Project Wiki

Wiki 只做 Knowledge View。

第一版页面：

```text
Project Wiki

├── Facts
├── Decisions
├── Progress
├── Open Questions
└── Evidence / Research Runs
```

首版可以只做：

```text
Facts
Decisions
```

每条展示：

```text
statement
status
version
publisher
source count
research_run
```

并可展开 Provenance。

---

# P3：Selective Routing Evaluation

在 P1/P2 稳定后做：

```text
All Normal
vs
All Deep
vs
Selective Deep
```

重点是验证 Deep 的适用边界，而不是一定上线自动 Routing。

---

# P4：Retrieval POC

参考 `research_copilot`，只做小实验。

## P4-A Technical-aware BM25 Tokenizer

当前：

```text
jieba.cut
+
user dictionary
```

实验：

```text
A. jieba.cut
B. jieba.cut_for_search
C. cut_for_search + technical regex tokens
```

技术 token 重点：

```text
RDMA
ib_write_bw
mlx5_0
CUDA-12.4
PCIe
192.168.1.1
commit hash
path/name
```

指标：

```text
Hit@5
MRR@5
NDCG
Coverage
Drift
Regression
Latency
```

有稳定增益才合入。

## P4-B Parent / Child Retrieval

如果长文档 context 问题仍明显，再做：

```text
Child
→ Retrieval

Parent
→ Generation Context
```

比较：

```text
Flat Chunk
vs
Parent / Child
```

指标：

```text
Recall
Faithfulness
Context Completeness
Latency
```

---

# P5：External Evidence Adapter

最后做。

复用已有 MCP plumbing，但不把 Deep Research 改成 ReAct。

第一版：

```text
Researcher Targeted Query
→ arXiv Adapter
→ EvidenceItem
→ EvidencePackage
→ Analyst
→ Reviewer
```

约束：

- 只读；
- 每个子问题 3～5 条；
- 短 timeout；
- failure fallback internal evidence；
- external content 视为 untrusted input；
- 不自动进入 Mem0；
- 不自动进入 Project Knowledge；
- Promotion 仍经过同一 Knowledge Gate。

---

# P6：Graph 仅保留为条件性增强

先实现普通数据库关系：

```text
knowledge_relations
```

支持：

```text
supports
contradicts
supersedes
derived_from
uses_config
```

只有真实需求出现复杂多跳查询时，再评估 Neo4j / Knowledge Graph。

---

# 11. 当前明确不应该做的事情

近期不要做：

```text
更多 Agent
Supervisor
动态 Worker
Memory Agent
Plan-ReAct-MCP 替换主链
SFT/DPO
Qdrant 迁移
Redis
MinIO
Neo4j
大 UI 重构
```

---

# 12. 推荐执行顺序

```text
Phase 0
完成 8 条真实 Browser Acceptance
        ↓
完成 Memory E2E
        ↓
人工 Calibration（有资源则做）
        ↓

Phase 1
Verified Knowledge Promotion
        ↓
Provenance
        ↓
Revoke / Supersede
        ↓
Derived Knowledge Safety
        ↓

Phase 2
Project Wiki
        ↓

Phase 3
Selective Routing Evaluation
        ↓

Phase 4
Technical-aware BM25 POC
        ↓
必要时 Parent / Child POC
        ↓

Phase 5
单一 External Evidence Adapter
        ↓

Phase 6
Graph（真实需求触发）
```

---

# 13. 建议提交粒度

```text
test: 完成多轮科研问答真实浏览器回归

test: 完成科研事实记忆端到端验收

feat: 增加可信科研知识提升与来源追溯

feat: 增加项目知识Wiki视图

eval: 评估Normal与Deep选择性路由

eval: 评估科研技术词BM25分词策略

eval: 评估Parent-Child长文档检索

feat: 接入只读外部科研证据源
```

---

# 14. 最终项目定位

不再定位为：

> “实验室 RAG 问答机器人”

更合适的是：

> **实验室科研知识协作 Agent**

最终架构：

```text
                        ┌──────── Normal RAG
                        │
Raw Documents ──────────┤
                        │
                        └──────── Deep Research
                                  │
                       Researcher → Analyst → Reviewer
                                  │
                             Research Run
                                  │
                           Verified Claims
                            ↙           ↘
                    Personal Memory   Knowledge Promotion
                                            │
                                      Project Knowledge
                                      ↙             ↘
                                   Wiki          Retrieval
                                                   │
                                                   └──→ Next Research
```

---

# 15. 简历与面试的评测表达方式

## Retrieval

可以表达：

> 在 24 条冻结多轮指代集上，Standalone Rewrite 将正例 Hit@5 从 86.7% 提升至 100%，MRR@5 从 0.867 提升至 0.933，且未引入 ACL 违规或正例回退。

## Deep Research

不要写：

> “Deep Research 全面提升回答质量。”

更准确：

> 构建 20 条科研任务、60 份冻结方案输出的声明级因果评测，发现完整 Deep Research 将 Claim Recall 提升至 0.734、Faithfulness 提升至 0.781，但带来约 5 倍延迟；进一步通过任务类别拆分确认其收益主要集中在复杂实验设计与多证据综合，因此保留为显式复杂研究模式。

## Cost / Quality Tradeoff

可以表达：

> 评估 Reviewer Actionability Gate 后发现其可降低约 14.6% 延迟和 16.0% 输入 Token，但使 Faithfulness 下降 4.4pp，超过预设 2pp 质量容差，因此依据冻结消融结果回退优化。

---

# 16. 下一条给 Codex 的执行 Goal

> 先完成当前科研助手的真实验收收尾：运行并固定 8 条冻结多轮样本的 Browser Acceptance 结果，并补一条真实 Research Fact → 用户确认 → Mem0 → 跨会话召回 → 删除/撤回 → ACL 降权后拒绝召回的端到端用例。完成后，不新增 Agent、不实现 Step-back、不接外部工具，开始设计 Verified Knowledge Promotion：将 Reviewer PASS、Evidence-backed 且经独立 Publish 动作确认的科研事实/决策结构化沉淀到项目 Knowledge Record，保留 Research Run、Claim、Source Provenance，并支持 active / superseded / revoked 生命周期及当前 ACL 校验。第一版不得引入 Neo4j、Qdrant、Redis、MinIO 或第二套 Memory 系统。
