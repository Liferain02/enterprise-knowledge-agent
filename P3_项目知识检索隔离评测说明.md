# P3 项目知识检索隔离评测说明

## 1. 为什么单独评测

项目知识不是普通原始文档。它是从 Research Run 的 Claim 经过 Reviewer、当前 ACL 和用户发布动作后形成的派生记录。如果直接把它写入原向量库，后续研究可能引用自己的派生结论，再次形成新的 Claim，造成自我强化。因此 P3 先做可关闭的补充检索，并用冻结样本验证边界。

## 2. 两个实验变体

### 2.1 Raw Documents Only

只使用现有 Chroma/Hybrid/Rerank 原始文档结果。这是产品默认路径，也是后续所有实验的基线。

### 2.2 Raw Documents + Active Project Knowledge

保留完整 raw top-k，再从当前 `project_id` 对应的 ResearchService 读取 `active` 记录，追加最多两个相关项目知识。项目知识不写入 Chroma，不改变原始文档排序。

## 3. 安全约束

1. 请求没有明确 `project_id` 时不读取项目知识。
2. 服务层使用当前用户重新验证项目成员、Research Run 和 Evidence ACL；验证失败直接不返回。
3. `revoked`、`superseded` 和来源不完整的记录被排除。
4. 每条适配结果都带有 `knowledge_record_id`、`research_run_id`、`claim_id`、`root_source_ids`、`knowledge_origin`。
5. `derived_only` 不能被适配为可检索的项目知识，也不能成为新的发布证据。
6. 普通文档的回答后二次 ACL 检查不会覆盖已经由项目服务确认的项目知识。

## 4. 指标定义

| 指标 | 定义 | 通过条件 |
| --- | --- | --- |
| raw recall | gold 原始来源在结果中的比例 | 混合检索不低于基线容差 |
| project knowledge hit | gold 项目知识记录被命中的次数 | 只统计 active |
| revoked/superseded hit | 已撤销或已替代记录命中次数 | 必须为 0 |
| ACL violation | 当前用户不可见记录命中次数 | 必须为 0 |
| provenance completeness | 项目知识结果五类 provenance 字段完整比例 | 必须为 1 |

## 5. 运行方式

```bash
conda activate agent-demo
python -m tests.eval.项目知识检索隔离评测
python -m tests.eval.项目知识检索隔离评测 --write
```

脚本使用内存冻结样本，不读取真实数据库，不调用 LLM 或网络。`--write` 只生成本地 `data/项目知识检索隔离评测结果.json`，该目录按项目规则被忽略。

## 6. 当前结论

当前冻结样本中 raw recall 保持不变，active 项目知识可以命中，撤销/替代命中为 0，ACL violation 为 0，provenance 完整率为 1。样本规模仍然很小，不能据此默认开启，因此配置保持关闭；后续只在增加真实、脱敏、冻结的科研失败样本后再复测。

## 7. 不做的事情

本阶段不新增 Agent、Redis、Qdrant、Neo4j、MinIO，不实现自动知识发布，不把派生知识当作原始 Evidence，也不因为单次命中就修改默认路由。
