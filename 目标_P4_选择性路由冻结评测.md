# P4 目标：选择性路由冻结评测

## 1. 目标

验证实验室科研助手是否应该把每个问题都交给 Deep Research。评测只比较路由策略，不改线上默认行为：

- `All Normal`：所有问题走普通 RAG。
- `All Deep`：所有问题走固定 Researcher、Analyst、Reviewer 链路。
- `Deterministic Selective Deep`：复用现有窄规则，只将跨资料综合、时间/冲突、前提核验和研究建议类问题送入 Deep。

## 2. 评测边界

- 复用 `is_complex_research_task`，不新增 Agent、不引入 LLM 路由器。
- 使用现有 41 条复杂科研样本，加上明确的普通问答对照样本。
- 本阶段只评估“是否该升级”的决策质量和成本代理，不运行三套完整模型生成，不把路由结果冒充最终答案质量。
- 线上仍保持用户显式选择 `research_mode=deep`，不自动切换。

## 3. 指标与门槛

- `routing_precision`：被选择为 Deep 的问题中，真正需要 Deep 的比例。
- `routing_recall`：真正需要 Deep 的问题被选择的比例。
- `routing_f1`：Precision 与 Recall 的调和平均。
- `over_route_count`：普通问题被升级的数量。
- `under_route_count`：复杂问题未升级的数量。
- `estimated_logical_calls`：Normal 按 2 次、Deep 按 8 次逻辑调用估算，只用于相对比较。

选择性路由只有在 Recall 不低于 0.95、F1 不低于 0.90，且相对 All Deep 明显减少估算调用时，才具备进入下一轮真实回答评测的资格。即使达到门槛，也不会在本阶段自动上线。

## 4. 停止规则

若选择性路由漏掉复杂问题，继续收窄规则会停止；若误升级过多但质量没有证据收益，则保留显式 Deep 模式。只有在冻结路由评测通过后，才考虑在脱敏盲测中比较真实回答质量、引用支持率、前提判断和延迟。

## 5. 运行

```bash
conda activate agent-demo
python -m tests.eval.选择性路由冻结评测
python -m tests.eval.选择性路由冻结评测 --write
```
