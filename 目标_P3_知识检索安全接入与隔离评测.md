# P3 目标：知识检索安全接入与隔离评测

## 1. 目标

在不新增 Agent、向量数据库或 Redis 的前提下，让已经由 Research Run、Reviewer、ACL 和用户操作确认的**有效项目知识**能够参与项目内问答，并证明它不会把系统带入“自己生成、自己引用、自己再次发布”的自我强化循环。

## 2. 明确边界

- 只有请求显式携带 `project_id` 时才允许读取项目知识。
- 只有 `active` 记录参与检索；`revoked` 和 `superseded` 永不进入检索结果。
- 读取入口必须复用当前项目 ACL、Research Run ACL 和 Evidence ACL；任一校验失败就不返回记录。
- 每条结果必须携带 `knowledge_record_id`、`research_run_id`、`claim_id`、`root_source_ids` 和 `knowledge_origin`。
- `derived_only` 记录不能发布为项目知识，也不能作为新的可发布证据。
- 开关默认关闭，Raw Documents Only 是默认基线；实验时显式打开混合检索。
- 不把项目知识写入 Chroma，不改变原始文档检索和默认回答路径。

## 3. 最小实现

1. 增加 `project_knowledge_retrieval_enabled` 配置。
2. 从现有 `ResearchService.list_knowledge_records` 读取 active 记录。
3. 将记录转换为结构化 `Document`，保留 statement、来源摘录和 provenance。
4. 只在项目知识开关打开且 `project_id` 非空时，与 raw retrieval 结果做少量合并去重。
5. 在检索测试中覆盖状态过滤、ACL、无项目上下文、来源链和默认关闭行为。
6. 用冻结小数据集比较 Raw Documents Only 与混合检索，记录 raw recall、项目知识命中、撤销/替代命中、ACL violation 和 provenance 完整率。

## 4. 完成门槛

- 默认开关关闭时现有全量测试不回归。
- active 知识可命中，撤销和替代知识命中数为 0。
- 无 `project_id`、无权限用户和来源无法复核时，项目知识命中数为 0。
- ACL violation 为 0，所有项目知识结果均有 root raw source。
- 混合检索不覆盖 raw source，且评测结果可复现。
- 完成 pytest、前端构建和真实项目问答验收后，才结束本 Goal。

## 5. 停止规则

若混合检索不能稳定提升 Gold Hit 或 Claim Faithfulness，或使 raw recall 超过容差下降，则保留实现和评测能力，但继续使用 Raw Documents Only，不为追求指标增加复杂基础设施。
