# 实验室科研助手知识演化任务书评估与实施计划

## 1. 总体评估

《实验室科研助手：知识演化与评测驱动优化任务书》的总体判断正确：项目下一阶段不应继续增加 Agent 或反复调整普通问答，而应把已经具备的 Research Run、Claim、Reviewer、Evidence、ACL 和用户确认串成受控知识生命周期。

任务书最有价值的判断有四项：

1. 个人 Memory 与项目 Knowledge 必须分离；
2. Wiki 只是结构化知识的视图，不能成为第二套事实源；
3. Graph 首先是关系语义，不等于引入 Neo4j；
4. 所谓“自进化”必须落到有人发布、可追溯、可撤销、按当前权限重新校验的知识演化。

这些判断与当前代码基础匹配，适合继续实现。

## 2. 已完成项

任务书 P0 已于 2026-09-04 完成：

- 8 组冻结多轮样本全部通过真实 Browser Acceptance；
- 8/8 路由正确，8/8 命中金标准来源；
- 16 次回答延迟 P50 为 4.44 秒、P95 为 9.94 秒；
- HTTP 和浏览器控制台错误均为 0；
- 真实完成 Research Fact → 用户确认 → Mem0 → 新会话召回 → 页面撤销 → 不再召回；
- ACL 降权 Recall Gate 已由集成测试覆盖；
- Qwen Judge 与 Codex 模拟校准已完成；
- 独立真人 Calibration 仍为 Pending。

因此不再重复投入 P0，不继续调整登录、增加指代 Agent 或扩建评测平台。

## 3. 任务书中需要修正的地方

### 3.1 首版只支持 fact

任务书建议首版支持 `fact / decision`，但当前 Deep Research Claim 协议只有：

```text
fact
comparison
inference
recommendation
limitation
```

直接加入 `decision` 会同时影响 Analyst 提示词、Reviewer 协议、确定性渲染、冻结评测和历史数据兼容。当前没有真实的 Decision 发布样本证明这项改动必要。

第一版只发布 `fact`。等真实科研流程出现“经过负责人确认的方案决策”后，再独立设计 Decision 来源与审批边界。

### 3.2 ReviewItem 门禁应采用负面排除

任务书写的是：

```text
Reviewer item supported = true
issue_type = none
```

当前 `ReviewReport.items` 主要表达问题，PASS 报告可以没有逐 Claim 的正面 item。如果强制寻找 `supported=true` 条目，会错误拒绝合法 PASS Claim。

实际门禁改为：

```text
ReviewReport.decision = PASS
acl_verified = true
不存在该 Claim 的 supported=false ReviewItem
```

### 3.3 Knowledge Retrieval 必须晚于结构化生命周期

如果创建记录后立刻把 Derived Knowledge 混入当前向量库，会提前引入两个风险：

- Derived Knowledge 与 Raw Document 排名互相干扰，现有 Retrieval 基线失效；
- 后续 Research 可能引用系统自己生成的知识，再把它发布成新事实。

因此先完成“存储、发布、追溯、撤销、替代”，再单独接入 active Knowledge Retrieval，并运行来源链与检索回归。

### 3.4 不增加独立 Knowledge Agent

Knowledge Promotion 是领域服务和数据生命周期，不需要新 Agent。发布条件必须由服务端确定性校验，LLM 只能生成候选 Claim，不能执行发布动作。

## 4. 最小产品模型

### 4.1 两类沉淀严格分离

```text
确认并记住
→ 用户个人 Mem0

发布为项目知识
→ 项目 Knowledge Record
```

二者使用不同按钮、接口、数据表和权限语义。

### 4.2 项目知识记录

首版字段：

```text
id
project_id
knowledge_type = fact
statement
status = active | superseded | revoked
version
research_run_id
claim_id
source_ids_json
created_by
published_by
created_at
updated_at
supersedes_id
```

项目可见范围不在记录上重复保存，而是始终继承项目当前 ACL。这样项目权限变化后无需同步更新每一条知识记录。

### 4.3 发布门禁

一次发布必须同时满足：

1. Research Run 已完成；
2. Research Run 必须属于项目；
3. 当前用户拥有项目写权限；
4. Reviewer 决策为 PASS 且 ACL 已验证；
5. Claim 类型为 fact；
6. Claim source_ids 非空；
7. 每个 Source ID 在当前可见 EvidencePackage 中存在；
8. 不存在 Reviewer 明确拒绝该 Claim 的条目；
9. 至少一条 Evidence 最终属于 raw document 或 external evidence；
10. 用户执行独立的“发布为项目知识”动作。

客户端不能提交 statement、source_ids 或 publisher，防止绕过 Research Run 伪造来源。

### 4.4 生命周期

```text
active
  ├── revoke    → revoked
  └── supersede → superseded
                     ↑
                新记录 active
```

撤销和替代只改变状态，不物理删除记录与 Provenance。

同一个 `research_run_id + claim_id` 只能生成一条知识记录。已撤销或已替代的 Claim 不能原地重新激活，应通过新的 Research Run 形成新版本。

### 4.5 当前 ACL

读取 Knowledge Record 时不能只相信发布时权限：

```text
当前用户可访问项目
+
当前用户可访问原 Research Run
+
Source IDs 在当前过滤后的 EvidencePackage 中仍完整存在
```

任一条件失败则 fail closed，不返回 statement 和 Provenance。

## 5. 分阶段实施计划

### 阶段一：可信项目知识记录

目标：完成不参与 Retrieval 的结构化生命周期。

实现：

- 新增 `research_knowledge_records` 表；
- 新增 Publish Gate；
- 新增发布、列表、详情、撤销和替代接口；
- Research Run 返回已发布 Claim 状态；
- 页面提供独立的“发布为项目知识”按钮；
- 增加服务层、API、ACL、幂等和生命周期测试。

验收：

- 无项目 Run 不可发布；
- 非项目写入者不可发布；
- REVISE、非 fact、无来源、来源不可见均不可发布；
- statement 与 source_ids 必须来自服务端 Research Run；
- 撤销后默认列表不可见，但审计查询仍可见；
- 替代后旧记录为 superseded，新记录版本递增且保留旧记录 ID。

### 阶段二：Project Wiki 最小视图

目标：让项目成员看到 Facts，而不是建设新的 Wiki 编辑器。

实现：

- 科研空间项目详情增加“可信知识”区域；
- 默认只显示 active；
- 展示 statement、version、publisher、source count；
- 展开后显示 Research Run、Claim 和 Evidence 标题；
- 有写权限的用户可撤销；
- 暂不支持自由编辑正文。

验收：

- 项目 ACL 与现有项目页一致；
- 普通成员只读；
- 无权限用户无法从 API 获取记录；
- 页面不复制生成另一份 Wiki 文本。

### 阶段三：安全接入 Retrieval

目标：让 active Project Knowledge 可以被后续问题引用，同时避免自我强化。

实现前先冻结小型数据集，对比：

```text
Raw Documents Only
vs
Raw Documents + Active Project Knowledge
```

只有满足以下条件才合入：

- Gold Hit 或 Claim Faithfulness 有稳定收益；
- Raw Document Recall 不下降超过预设容差；
- revoked / superseded 命中为 0；
- ACL violation 为 0；
- 每条 Derived Knowledge 保留 root raw source；
- Derived-only Claim 禁止再次 Promotion。

第一版可以在现有检索结果之后合并少量结构化记录，不新建向量数据库。

### 阶段四：Selective Routing Evaluation

只做冻结实验，不先上线自动路由：

```text
All Normal
vs
All Deep
vs
Deterministic Selective Deep
```

如果质量、Faithfulness 和成本没有同时满足门槛，继续保留显式 Deep 模式。

### 阶段五：条件性 POC

按真实失败样本触发：

- 技术词 BM25 分词；
- Parent/Child 长文档检索；
- 单一只读外部证据源。

Neo4j、Redis、Qdrant、MinIO、SFT/DPO 和新增 Agent 不在当前计划内。

## 6. 提交与停止规则

建议提交顺序：

```text
feat: 增加可信项目知识发布与生命周期
feat: 增加项目可信知识视图
eval: 验证项目知识检索收益与安全边界
eval: 评估Normal与Deep选择性路由
```

每个阶段必须先通过自动测试和最小真实浏览器验收，再进入下一阶段。如果阶段三没有稳定检索收益，应保留 Knowledge Record 与 Wiki，但停止把 Derived Knowledge 接入 RAG。

## 7. 阶段一实施记录

### 7.1 服务端闭环

2026-09-04 已完成第一批服务端能力：

- `research_knowledge_records` 保存项目知识、状态、版本和来源关系；
- `POST /api/v1/research/runs/{run_id}/claims/{claim_id}/publish-knowledge` 负责普通发布；
- `GET /api/v1/research/projects/{project_id}/knowledge` 负责按状态列出记录；
- `GET /api/v1/research/knowledge/{record_id}` 返回当前 ACL 下可验证的完整来源；
- `POST /api/v1/research/knowledge/{record_id}/revoke` 执行幂等撤销；
- `POST /api/v1/research/projects/{project_id}/knowledge/{record_id}/supersede` 使用另一条可信 Claim 创建新版本并替代旧版本。

普通发布接口不接受正文、来源、发布人或替代目标。独立替代接口也只接受新的 `run_id` 和 `claim_id`，所有事实内容和 Provenance 都由服务端从 Research Run 读取。

详情中的每条 Provenance 包含 `source_id`、来源标题、当前可访问的证据片段 `excerpt` 和最小定位字段 `locator`（优先使用 page/section/chunk_id，缺失时回退为 source_id）。如果当前用户无法重新验证任一来源，服务端不返回整条知识详情，列表也会 fail closed。

发布门禁已经确定性检查：完成态、项目归属、项目写权限、Reviewer PASS、显式 `acl_verified=true`、fact 类型、来源完整、负面 ReviewItem、非纯 Derived 来源和重复发布幂等。

### 7.2 自动测试证据

`tests/test_research_workspace.py` 当前包含 30 个通过用例，其中新增覆盖：

- 正常发布、来源追溯和重复发布；
- 无项目、非 PASS、ACL false、ACL 缺失、非 fact、无来源和不存在来源；
- 负面 Reviewer item 与 Derived-only 来源；
- 公开项目只读用户不可发布；
- 撤销与重复撤销；
- 同项目替代、重复替代、跨项目替代和替代非 active 记录；
- 文档 ACL 降权后详情与列表 fail closed；
- HTTP 请求伪造 statement/source_ids 不会改变服务端记录。

运行结果：

```text
30 passed
```

### 7.3 最小前端闭环

2026-09-04 已在现有页面内补齐交互，没有增加独立管理后台：

- 科研空间的项目详情增加“可信项目知识”区域；
- 默认只显示 active，用户可切换查看全部历史版本；
- 每条记录展示 statement、version、status、publisher 和来源数；
- 展开详情时重新请求服务端，由当前 ACL 校验后展示 Research Run、Claim 和 Evidence；
- 有项目写权限的用户可以撤销 active 记录；
- Research Run 的 fact 行可以选择一条 active 知识作为替代目标；
- 发布或替代失败时直接展示服务端门禁原因，不在前端复制门禁规则。

前端验证结果：

```text
vue-tsc 通过
vite build 通过
tests/test_research_workspace.py: 30 passed
```

### 7.4 真实浏览器与全量验收

2026-09-04 使用项目实际配置启动前后端，并用真实 Chromium 完成：

1. 输入账号密码并点击登录；
2. 打开带有 PASS fact 的 Research Run；
3. 点击“发布为项目知识”；
4. 进入科研空间并在刷新后确认记录仍存在；
5. 展开来源，确认 Research Run、Claim 和原始 Evidence 可追溯；
6. 点击撤销，确认 active 列表不再显示、全部版本中状态为 revoked；
7. 发布另一条 active 基线；
8. 从新 Research Run 选择该基线并点击替代；
9. 确认旧版本为 superseded v1、新版本为 active v2。

桌面视口和 390px 移动端均通过。移动端检测结果为 `bodyWidth=viewport=390`，项目知识记录没有横向越界。最终浏览器结果：

```text
HTTP 4xx/5xx: 0
Console error: 0
桌面布局遮挡: 0
移动端布局遮挡: 0
```

重启到最新构建后再次展开项目知识详情，页面实际显示了研究问题、Research Run、Claim、`source_id=S1` 和证据片段“复现实验测得 RDMA 吞吐量为 91 Gbps。”，证明 Provenance 并非只有数据库字段而是可在产品中查看。

最终自动回归（补充 Provenance 片段后复核）：

```text
tests/test_research_workspace.py: 30 passed
全量 pytest: 356 passed
vue-tsc: passed
vite build: passed
```

阶段一已经达到“可发布、可追溯、可撤销、可替代、可测试”的验收标准。项目知识仍未接入普通 RAG，下一步应先做阶段二的最小 Wiki 使用体验评估，再按阶段三要求运行 Retrieval 隔离实验。

## 8. 阶段二实施记录

### 8.1 Wiki 视图收敛

P2 长期目标已写入 `goal_p2.md`。现有科研空间继续复用 `research_knowledge_records`，没有增加 Wiki 数据表或编辑器。项目卡片补充当前 active 知识数量，项目详情保留 active / all 两种视图，条目展示版本、状态、发布者、更新时间和来源数量。

Wiki 生命周期操作与普通成员读取分离：项目负责人、创建者和管理角色可以撤销或替代；普通项目成员仍可读取当前可见知识，但不会看到撤销按钮，服务端也会拒绝越权请求。实验记录等既有项目协作权限不受影响。

### 8.2 契约复核与测试

控制器现在兼容真实 FastAPI 注入的字符串和测试/内部直接调用时的 `Query(default=...)` 对象，默认状态始终为 `active`。新增测试覆盖：

- 项目 active 知识计数在发布和撤销后准确变化；
- 普通成员可读但不可撤销 Wiki 知识；
- 列表默认 active、切换 all 和非法状态的 API 错误映射；
- 服务端详情与来源 ACL 保持 fail closed。

本阶段定向结果：

```text
tests/test_research_workspace.py: 32 passed
vue-tsc / vite build: passed
```

P2 尚未完成真实浏览器最终验收和全量回归；在此之前不进入 P3 Retrieval。

### 8.3 P2 最终验收

2026-09-04 重启最新服务后使用真实 Chromium 验收项目知识 Wiki：

- 管理员登录后进入科研空间，项目卡片显示 `1 条有效知识`；
- 项目详情默认 active 列表为 1 条，切换“全部版本”后为 3 条；
- 历史记录正确显示 `active v2`、`superseded v1` 和 `revoked v1`；
- 展开 active 记录可看到研究问题、Research Run、Claim、`source_id=S1` 和 Evidence 片段；
- 刷新页面后 active/all 状态和记录数量保持一致；
- 普通成员的撤销权限由服务端拒绝，页面不显示管理按钮；
- 390px 移动视口无横向溢出，知识条目边界均在视口内。

真实浏览器结果：

```text
桌面 active: 1
桌面历史: 3
移动端 bodyWidth=viewport=390
HTTP 4xx/5xx: 0
Console error: 0
```

最终自动验证：

```text
全量 pytest: 358 passed
tests/test_research_workspace.py: 32 passed
vue-tsc / vite build: passed
```

P2 已完成。Wiki 仍只展示和追溯项目知识记录，没有接入普通 RAG；下一阶段必须先进行 Raw Documents 与 Active Project Knowledge 的隔离检索对照实验，确认不会发生自我强化后才能进入 P3。
