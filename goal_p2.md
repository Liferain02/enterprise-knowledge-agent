# Goal P2：完成实验室科研助手的项目知识 Wiki 与详情展示

## 1. 目标

把 P1 已经具备生命周期的项目知识记录，整理成实验室成员可以快速浏览、筛选和追溯的最小 Wiki 视图。

Wiki 只是 `research_knowledge_records` 的产品视图，不创建第二套事实源，不允许自由编辑正文，不改变发布门禁，也不把项目知识接入普通 RAG。

## 2. 产品定位

本阶段服务的是实验室科研协作：成员需要知道当前项目有哪些已经确认的事实、每条事实来自哪次 Research Run、由谁发布、现在是否有效，以及在权限变化后能否继续查看来源。

核心使用路径：

```text
科研空间
  -> 选择项目
  -> 浏览当前有效知识
  -> 查看版本、发布者和来源数量
  -> 展开详情
  -> 查看 Research Run / Claim / Evidence 片段
  -> 按权限执行撤销或返回历史版本
```

## 3. 范围

### 3.1 必须完成

- 项目详情展示“可信项目知识”区域；
- 默认只展示 `active` 记录；
- 支持切换全部历史状态；
- 每条记录展示 statement、版本、状态、发布者、更新时间和来源数量；
- 详情展示 Research Run、Claim、Evidence 标题、片段和定位；
- 详情读取必须经过当前项目 ACL 和 Evidence ACL；
- 无权限时 fail closed，不展示残缺 statement 或来源；
- 有项目写权限的用户可以撤销 active 知识；
- 普通成员只读；
- 页面错误直接展示服务端原因；
- 桌面和移动端没有布局溢出或控件遮挡。

### 3.2 明确不做

- 不新增 Knowledge Agent；
- 不新增自由编辑器或第二套 Wiki 数据表；
- 不自动合并知识；
- 不接入普通 RAG；
- 不引入 Redis、Neo4j、Qdrant、MinIO；
- 不做复杂审批、评论、标签系统或全文搜索；
- 不修改 P1 发布门禁与 Research Team 主链。

## 4. 实施步骤

### 第一步：现状审查

确认 P1 API、项目 ACL、来源详情和现有科研空间页面是否满足最小 Wiki 入口；识别真正缺失的交互和测试，不重复实现已有能力。

### 第二步：服务端契约复核

复核列表、详情和撤销接口：

- 默认状态是 `active`；
- `all` 能看到历史状态但不能绕过权限；
- 来源详情始终从原 Research Run 当前可访问 Evidence 重建；
- 跨项目记录、降权用户和不存在记录不会泄漏内容；
- 读接口不会修改知识状态。

### 第三步：页面最小视图

在已有科研空间项目详情中实现：

- active / all 状态切换；
- 紧凑知识条目；
- 展开详情和来源片段；
- 撤销按钮与确认；
- 空状态、加载状态、错误状态；
- 移动端单列布局。

页面不复制服务端门禁，只根据接口响应更新状态。

### 第四步：测试

自动测试至少覆盖：

- active 默认列表和 all 历史列表；
- 详情来源字段完整；
- ACL 降权后详情 fail closed；
- 无项目访问权限；
- 普通成员不可执行撤销；
- 撤销后的列表状态；
- 前端关键入口和错误提示契约；
- 前端构建。

### 第五步：真实验收

使用真实 Chromium：

1. 登录实验室账号；
2. 进入科研空间并选择项目；
3. 确认默认显示 active 知识；
4. 切换全部版本；
5. 展开一条知识，确认 Research Run、Claim、Evidence 片段和定位；
6. 刷新页面，确认状态一致；
7. 使用有写权限账号撤销 active 知识；
8. 确认 active 列表消失、历史列表显示 revoked；
9. 使用 390px 移动视口检查无横向溢出；
10. 收集 HTTP 错误、控制台错误和截图证据。

## 5. 每个小任务的交付规则

每个小任务完成后必须：

1. 更新中文文档，记录实现边界和测试结果；
2. 补充或更新自动测试；
3. 运行与改动相关的测试；
4. 执行 `git add .`；
5. 执行 `git commit`，提交信息说明阶段性结果。

## 6. 完成标准

只有同时满足以下条件，P2 Goal 才能标记完成：

- Wiki 视图使用已有项目知识记录，没有第二套事实源；
- 默认 active、历史 all、详情追溯和撤销均可用；
- 项目 ACL 和来源 ACL 无泄漏；
- 服务端与前端测试齐全；
- `npm run build` 通过；
- 全量 pytest 通过；
- Chromium 桌面和 390px 移动端真实验收通过；
- HTTP 错误和控制台错误为 0；
- 中文实施文档已更新；
- 已执行 `git add .` 和 `git commit`。

完成 P2 后，才评估 P3“知识接入检索与不自我强化实验”。
