# P4 目标：人工盲评模拟与校准

## 1. 目的

自动指标显示 Deep 的复杂问题关键词覆盖更高，但引用支持 proxy 下降且延迟明显增加。此阶段用已落盘的 6 条脱敏回答，模拟一次标签隐藏的人工评审，检查自动指标是否与人类可解释判断同向。

## 2. 评审规则

每个答案按 0～2 分评估：

- 正确性：是否准确回答问题，是否把计划、观察和结论混淆。
- 完整性：是否覆盖题目要求的关键点和必要限制。
- 证据依据：是否使用正确来源并让结论可追溯。

总分为 0～6 分。评分依据只使用题目、预设关键点、参考资料范围和答案文本，不读取方案标签、延迟或调用次数。

## 3. 评审性质

这是 Codex 执行的规则化模拟评分，产物显式标记 `review_kind=simulated_human`。它不能替代实验室成员或领域专家的独立人工评审，也不能在面试材料中写成真人盲评。

## 4. 决策门槛

只有 Deep 在复杂样本的平均总分至少高于 Normal `0.5` 分，且证据依据维度不下降超过 `0.2` 分，才进入下一轮独立真人盲评。否则继续停止自动路由上线，保留显式 Deep 模式。

## 5. 运行方式

```bash
conda activate agent-demo
python -m tests.eval.人工盲评模拟与校准 --write
```

脚本只读取 `data/脱敏真实回答评测结果.json`，不调用模型、不写入知识库、不修改线上路由。
