# 实验室科研助手：参考 research_copilot 后的剩余改造分析

> 项目：`Liferain02/enterprise-knowledge-agent`
>
> 目的：对照此前从参考项目 `research_copilot` 中筛选出的可借鉴设计，结合当前最新实现，明确哪些已经完成、哪些仍未实现，以及后续优先级。
>
> 核心原则：**不照搬参考项目的 Plan-ReAct-MCP 主链，不增加无意义 Agent；只吸收真正能增强当前“可信 RAG + 有界 Deep Research”架构的能力。**

---

# 1. 总体结论

此前从参考项目中筛选出的主要借鉴点包括：

1. Research Run / Agent Trace
2. 分层长期记忆
3. Standalone Query Rewrite
4. Step-back Query Rewrite
5. External Research Tools / MCP
6. 明确不照搬 Plan-ReAct、多 Agent 扩张、Tool-use SFT/DPO 等设计

结合当前最新代码，整体完成度大致如下：

| 模块 | 当前状态 | 完成度 |
|---|---|---:|
| Research Run / Trace | 基本完成 | 90% |
| Evidence-backed Memory | 核心完成 | 85% |
| Standalone Query Rewrite | 实现完成，评测待固定 | 80% |
| Step-back Query Rewrite | 未实现 | 0% |
| External Research Tools | 未实现 | 0% |
| 主架构收敛 | 已完成 | 95% |

整体可认为：

> **核心架构改造已经完成约 70%～75%，接下来重点不再是堆功能，而是补齐评测、Step-back、项目级记忆和外部证据接入。**

---

# 2. Research Run / Trace

## 2.1 当前已经实现

当前 Deep Research 已经具备完整的结构化运行状态：

```text
Researcher
    ↓
Analyst
    ↓
Reviewer
    ↓
最多一次 Revision
    ↓
Generation
```

运行过程中已经记录：

```text
EvidencePackage
AnalysisReport
ReviewReport
research_trace
research_team_metrics
generation_metrics
failure attribution
stage latency
```

这些状态已经不再只是测试数据，而是被正式持久化为：

```text
Research Run
```

Research Run 当前已经可以保存：

```text
question
session_id
project_id
user_id

EvidencePackage
AnalysisReport
ReviewReport
Research Trace
Metrics

final_answer
source_cards
```

并且已经提供 API 和前端入口。

---

## 2.2 ACL 处理已经比较完整

当前 Research Run 读取时会重新根据当前用户权限执行 ACL。

这意味着：

```text
过去有权限
→ Research Run 被保存

之后角色降级 / 项目权限变化
→ 再次读取 Research Run
→ Evidence 重新鉴权
```

如果发现历史 Evidence 当前不可见：

```text
Evidence 隐藏
Source Cards 隐藏
AnalysisReport 隐藏
ReviewReport 隐藏
Final Answer 保守隐藏
```

这是当前项目非常重要的可信性设计。

---

## 2.3 当前还缺什么

Research Run 核心已经完成，不建议继续做大规模架构改造。

剩余主要是 UI 产品化：

```text
Researcher
- subquestions
- targeted queries
- evidence count

Analyst
- claims
- conflicts
- premise

Reviewer
- decision
- issue type
- missing evidence

Revision
- 是否发生
- revision count

Metrics
- retrieval calls
- llm calls
- latency
```

建议将这些内容在前端以更清晰的折叠式 Trace 展示。

但这一项属于：

> **P2 Polish，而不是核心缺口。**

---

# 3. 分层长期记忆 / Evidence-backed Memory

## 3.1 当前已经形成三层 Memory

当前项目事实上已经形成：

### Working Memory

```text
LangGraph conversation state
+
rolling summary
```

用于：

- 当前会话上下文
- 最近几轮对话
- 长会话压缩

---

### Episodic Memory

主要由：

```text
Research Run
```

承担。

目前同题 Deep Research 还可以复用历史成功 Run 的：

```text
subquestion plan
```

但不会直接复用旧答案和旧 Evidence，而是重新按当前 ACL 检索。

这实际上已经是一个比较合理的：

> **Research Episode Memory**

---

### Semantic Memory

目前采用：

```text
Reviewer PASS
+
claim_type = fact
+
有效 source_ids
+
ACL verified
+
用户显式确认
        ↓
Mem0 infer=False
```

实现长期科研事实记忆。

---

## 3.2 当前 Evidence-backed + ACL-aware Memory 已经闭环

科研事实写入 Mem0 时已经带有：

```text
memory_type = confirmed_research_fact
scope = research
project_id
research_run_id
claim_id
source_ids
review_decision = PASS
user_confirmed = true
verified = true
```

召回时不再是：

```text
Mem0.search
→ 直接注入 Prompt
```

而是：

```text
Mem0.search
    ↓
Recall Gate
    ↓
普通 preference / conversation
    → 正常使用

confirmed_research_fact
    ↓
重新验证 Research Run
    ↓
重新验证 Claim
    ↓
重新验证 source_ids
    ↓
重新执行当前 ACL
    ↓
通过才允许注入
```

因此现在已经形成了比较清楚的职责边界：

```text
Mem0
→ 负责 Retrieval

Research Run + Evidence + Reviewer + ACL
→ 负责 Trust
```

---

## 3.3 当前还缺：Project-scoped Research Memory

目前 Mem0 仍主要按：

```text
user_id
```

做召回。

因此存在：

```text
Alice 是 Project A 成员

Alice 确认：
“实验最终采用配置 B”
        ↓
该事实存进 Alice 的 Mem0

Bob 同样属于 Project A
        ↓
Bob 当前不会自然召回 Alice 确认的项目事实
```

但从实验室科研协作角度，更合理的是：

```text
User Memory
├── preference
├── conversation
└── personal context

Project Research Memory
├── verified fact
├── decision
├── progress
└── open question
```

当前已经在 metadata 中保存：

```text
project_id
scope = research
```

但还没有真正实现：

> **同项目成员共享 verified research fact。**

这是后续值得补的一项。

---

## 3.4 一个可以暂时接受的边界

当前 Recall ACL 主要基于：

```text
历史 Research Run 中保存的 Evidence metadata
+
当前 User ACL
```

因此可以处理：

```text
PI → student
被移出 Project
```

等“用户权限变化”。

但如果是：

```text
某文档过去 public
→ 后来文档本身改成 restricted
```

而 Research Run 中仍保留旧 Evidence metadata，则不一定能感知源文档 ACL 的变化。

更严格的最终方案应该是：

```text
source document id
    ↓
查询当前文档 metadata
    ↓
Current User + Current Document ACL
    ↓
Allow / Reject
```

这一项目前不建议立即扩展，可以作为当前设计 limitation 保留。

---

# 4. Standalone Query Rewrite

## 4.1 当前代码已经完成

Standalone Query Rewrite 当前已经实现：

```text
检测多轮指代 / 省略
        ↓
保留 Original Query
        ↓
追加 Standalone Query
        ↓
Multi-query Retrieval
        ↓
RRF merge
        ↓
ACL
        ↓
Rerank
```

并且设计比较克制：

- 不是所有问题都 Rewrite；
- 使用明确触发规则；
- Query 数量有限制；
- 不恢复 CRAG 无限循环；
- Original Query 必须保留。

---

## 4.2 当前已经有冻结评测集

目前已有一套多轮指代数据集，覆盖：

```text
pronoun
ellipsis
comparison
no_rewrite negative cases
```

并设置了：

```text
Hit@1
Hit@5
MRR@5
NDCG@5
Trigger Precision
Trigger Recall
Trigger F1
ACL violations
Positive Regression Rate
Negative Changed Rate
Latency
```

以及预定义的 Gate。

这个设计是正确的。

---

## 4.3 当前真正缺的是：实验结果

现在状态应该定义为：

> **Implementation Complete / Evaluation Pending**

目前有评测脚本，但还需要真正执行：

```bash
python -m tests.eval.run_multi_turn_standalone_ablation
```

然后固定结果。

不能在简历、README 或项目介绍中直接写：

```text
MRR 提升 xx%
Hit@5 提升 xx%
```

除非真实实验已经跑出并保存。

---

## 4.4 后续还可以增强数据集难度

当前多轮数据集中很多 case 是：

```text
User Query 1
→ User Follow-up
```

例如：

```text
RDMA 实验前需要记录什么？
→ 它还要求做什么？
```

更难的真实场景是：

```text
User:
RDMA 实验有什么异常？

Assistant:
主要有三个：
1. ...
2. NUMA 绑核异常
3. ...

User:
第二个后来怎么解决的？
```

这种：

```text
assistant-dependent coreference
ordinal reference
long-distance reference
```

目前更难处理。

后续可以新增 10～15 条这类 case，提高评测可信度。

---

# 5. Step-back Query Rewrite

## 5.1 当前尚未实现

这是目前从参考项目中借鉴、但还没有落地的最明确能力。

适合场景：

```text
为什么 RDMA 延迟突然升高？
```

除了原问题：

```text
Original:
为什么 RDMA 延迟突然升高？
```

增加：

```text
Step-back:
影响 RDMA 延迟的主要软硬件因素有哪些？
```

然后：

```text
Original + Step-back
        ↓
一次 Multi-query Retrieval
        ↓
RRF
        ↓
ACL
        ↓
Rerank
```

---

## 5.2 不允许做成循环 CRAG

禁止：

```text
retrieve
→ judge
→ rewrite
→ retrieve
→ judge
→ rewrite
...
```

仍然保持：

> **Selective + Single-pass Retrieval**

---

## 5.3 Step-back 必须配套消融

后续至少比较：

```text
A. Original only
B. Original + Standalone
C. Original + Step-back
D. Original + Standalone + Step-back
```

指标继续使用：

```text
Hit@k
MRR
NDCG
Latency
Query Count
ACL Violation
Regression Rate
```

如果 Step-back 没有带来稳定收益，就不进入默认主链。

---

# 6. External Research Tools / MCP

## 6.1 当前尚未实现

这一项也是最初计划中的剩余内容。

目标不是把整个系统改造成 MCP Agent，而是让 Deep Research 可以获取：

```text
Internal Evidence
+
External Evidence
```

例如：

```text
arXiv
Semantic Scholar
GitHub
Web Search
```

---

## 6.2 推荐实现方式

不要修改 Analyst / Reviewer 协议。

只增加一个薄的：

```text
Research Tool / Evidence Adapter
```

例如：

```python
class ResearchTool(Protocol):
    name: str
    read_only: bool

    async def search(
        self,
        query: str,
        limit: int = 5,
    ) -> list[EvidenceItem]:
        ...
```

所有外部结果最终统一转成：

```text
EvidenceItem
```

继续进入：

```text
EvidencePackage
    ↓
Analyst
    ↓
Reviewer
    ↓
Generation
```

因此后面的 Agent 完全不需要关心：

```text
Evidence 是来自 Chroma
还是 arXiv
还是 GitHub
还是 Web
```

---

## 6.3 第一版只接一个工具即可

不要一次接：

```text
arXiv
Semantic Scholar
GitHub
Web
```

全部。

第一版建议只证明：

```text
External Result
    ↓
Evidence Adapter
    ↓
EvidencePackage
    ↓
Analyst
    ↓
Reviewer
```

链路成立。

例如优先接：

```text
arXiv
```

或者：

```text
GitHub read-only search
```

即可。

---

# 7. 哪些参考项目设计明确不需要实现

以下内容不是“还没做”，而是经过对比后明确认为：

> **不应该照搬。**

---

## 7.1 不实现默认 Plan-ReAct-MCP 主链

参考项目偏：

```text
Plan
→ per-node ReAct
→ Tool / MCP
→ Observation
→ ReAct loop
```

当前项目不应该改成这种默认架构。

继续保持：

```text
Normal
→ Deterministic Planner
→ RAG / General / Operation

Deep
→ Researcher
→ Analyst
→ Reviewer
→ ≤1 Revision
```

---

## 7.2 不增加 Supervisor / 动态 Worker

不新增：

```text
Supervisor Agent
Search Agent
Memory Agent
Citation Agent
Tool Agent
Manager Agent
```

Service / Adapter 可以增加，但不要为了概念包装成 Agent。

---

## 7.3 不做 Tool-use SFT / DPO

参考项目中的 Tool-use SFT / DPO 更偏 demo。

当前科研助手没有：

- 高质量 Tool-use 数据；
- 清晰基线；
- 明确业务收益；

因此当前阶段不做。

---

## 7.4 不为了“架构完整”引入 MinIO / Redis 等中间件

如果后续真的有：

```text
大文件
断点续传
分布式任务
```

再引入。

不能因为参考项目用了，就增加技术栈。

---

# 8. 当前真正剩余的四项工作

现在剩余工作可以压缩成四项。

---

## P0：先跑 Standalone 正式消融

不要先继续写功能。

先执行：

```text
Baseline
vs
Standalone
```

拿到真实：

```text
Hit@1
Hit@5
MRR@5
NDCG
Latency
Trigger F1
Regression
ACL Violation
```

如果 Gate PASS：

```text
正式保留 Standalone
```

如果 FAIL：

```text
分析 failure cases
必要时关闭 Standalone
```

---

## P1：实现 Selective Step-back Query

Standalone 评测完成后，再实现：

```text
Selective Step-back
```

并做：

```text
Original
vs
Standalone
vs
Step-back
vs
Full
```

消融。

---

## P2：实现 Project-scoped Research Memory

让：

```text
Alice 在 Project A 确认 verified fact
```

可以被：

```text
Project A 当前有权限的 Bob
```

安全召回。

重点仍然要保持：

```text
project ACL
source ACL
Evidence-backed
fail closed
```

---

## P3：接入一个 External Research Tool

第一版只接：

```text
1 个 read-only tool
```

验证：

```text
External Evidence
→ Evidence Adapter
→ EvidencePackage
→ Analyst
→ Reviewer
```

即可。

不需要第一版就做完整 MCP 平台。

---

# 9. 推荐最终执行顺序

```text
Phase 1
跑 Standalone Ablation
        ↓

Phase 2
实现 Selective Step-back
        ↓
做 Rewrite Ablation
        ↓

Phase 3
Project-scoped Research Memory
        ↓

Phase 4
External Research Tool
```

不要反过来：

```text
先 MCP
先更多 Agent
先更多工具
```

因为当前项目最缺的已经不是功能数量，而是：

```text
Evidence
Evaluation
Auditability
Memory Correctness
```

---

# 10. 当前项目的最终定位

随着 Research Run 和 Evidence-backed Memory 完成，项目已经从最初的：

```text
Hybrid RAG + Multi-Agent
```

逐渐收敛成：

```text
ACL-aware Hybrid RAG
+
Bounded Claim-level Deep Research
+
Auditable Research Run
+
Evidence-backed Long-term Memory
+
Selective Multi-turn Query Rewrite
```

下一阶段目标应该继续围绕：

> **让每一个新增能力都能被评测、复盘和安全约束。**

而不是继续增加 Agent 数量。

---

# 11. 一句话总结

对照 `research_copilot`，当前真正还没有完成的核心能力只剩：

```text
1. Standalone 的真实消融结果
2. Selective Step-back Query
3. Project-scoped Research Memory
4. External Research Tool / Evidence Adapter
```

而：

```text
Plan-ReAct-MCP 主链
更多 Agent
Tool-use SFT/DPO
MinIO 等中间件
```

属于：

> **明确不需要照搬的内容，而不是待办项。**
