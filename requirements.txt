# ============================================================
# LlamaIndex（Tier 1 开源复用：文档处理 + 分块 + 检索）
# 与 LangChain 完全兼容，用于更成熟的文档解析和分块算法
# ============================================================
llama-index>=0.14.0                    # 主包（自动安装 llama-index-core）
llama-index-llms-dashscope>=0.6.0       # 阿里通义千问 LLM 集成
llama-index-embeddings-dashscope>=0.5.0 # 通义千问 Embedding 集成
llama-index-vector-stores-chroma>=0.5.0 # ChromaDB 向量存储（LangChain 兼容）
llama-index-postprocessor-cohere-rerank>=0.8.0  # Cohere Rerank（比 BGE 更轻量）
llama-index-retrievers-bm25>=0.7.0      # BM25 检索器（替代手写 BM25）

# ============================================================
# Unstructured（Tier 1 开源复用：多格式文档解析）
# 支持 PDF/DOCX/HTML/CSV/Excel/PPT/图片 等全格式
# ============================================================
unstructured>=0.22.0                    # 多格式文档解析（自动包含 pdf/docx/xlsx）
# unstructured-inference  # 深度学习模型加速（可选）
# pillow-heif              # HEIF/HEIC 图片格式支持（可选）

# ============================================================
# LangGraph（Agent 工作流，保持）
# ============================================================
flashrank>=0.2.0                       # 轻量级 Rerank（可选，配合 Cohere 使用）

# LangChain 核心生态（保持不变）
langchain==1.2.10
langchain-community==0.4.1
langchain-text-splitters==1.0.0
langchain-chroma==1.1.0        # LangChain ChromaDB 集成

langchain-openai==1.1.10

# 新增：阿里通义千问官方 SDK（必须）
dashscope>=1.20.0          # 最新版建议 >=1.20，兼容 qwen3.5-flash 等

# 新增：Mem0 AI Memory
mem0ai>=0.1.0              # Mem0 智能记忆管理层

# 新增：Reranker 和混合检索
sentence-transformers>=2.2.0   # BGE Reranker 本地模型
rank-bm25>=0.2.2               # BM25 检索（备用，bm25s 优先）
bm25s>=1.0.0                    # Tier 1 BM25：Rust+Python，10x faster，内置中文分词
jieba>=0.42                     # 中文分词

# LangGraph（Agent 工作流，保持）
langgraph==1.0.9
langgraph-prebuilt==1.0.8  # LangGraph 预构建节点
langgraph-checkpoint-sqlite==3.0.3   # LangGraph SQLite 持久化 Checkpointer
aiosqlite==0.22.1          # AsyncSqliteSaver 依赖的异步 SQLite 驱动

# MCP (Model Context Protocol)
mcp==1.6.0                # MCP 协议支持

# 向量数据库（不变）
chromadb==1.5.1

# Web 框架 + SSE（不变）
fastapi==0.115.0
uvicorn==0.30.1
sse-starlette==2.1.0

# 配置 & 环境变量（不变）
pydantic==2.8.2
pydantic-settings==2.13.1
python-dotenv==1.0.1

# 工具 & 文档解析（不变）
numpy>=1.26.0
httpx==0.27.0
aiofiles==23.2.1
beautifulsoup4==4.12.3
lxml==5.3.0
pypdf==5.1.0
python-docx==1.1.2
docx2txt>=0.8
tiktoken==0.8.0           # 可选保留，如果还想用 OpenAI tokenizer 做 token 计数

python-multipart==0.0.12
python-frontmatter==1.1.0

# Auth（JWT）
python-jose[cryptography]==3.4.0
pwdlib[argon2]==0.3.0

# ============================================================
# Redis（评估缓存持久化 + 会话缓存）
# ============================================================
redis[hiredis]==5.2.0

# ============================================================
# 评估依赖（可选）
# ============================================================

# ============================================================
# 多模态文档处理依赖（可选）
# 用于表格提取、图片 OCR 等
# ============================================================
pymupdf>=1.23.0       # PyMuPDF 用于 PDF 表格和图片提取
openpyxl>=3.1.0       # Excel 表格提取
Pillow>=10.0.0        # 图片处理（OCR 需要）
pytesseract>=0.3.10   # OCR 文字识别（需要系统安装 tesseract）
# ============================================================
# 生产级功能增强
# ============================================================
tenacity>=8.2.0          # 重试机制 + Circuit Breaker
schedule>=1.2.0          # 定时任务（备份调度）
