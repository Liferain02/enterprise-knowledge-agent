# LangChain 核心生态（保持不变）
langchain==1.2.10
langchain-community==0.4.1
langchain-text-splitters==1.0.0
langchain-chroma==1.1.0        # LangChain ChromaDB 集成

langchain-openai==1.1.10

# 新增：阿里通义千问官方 SDK（必须）
dashscope>=1.20.0          # 最新版建议 >=1.20，兼容 Qwen-Max 等

# LangGraph（Agent 工作流，保持）
langgraph==1.0.9
langgraph-prebuilt==1.0.8  # LangGraph 预构建节点

# MCP (Model Context Protocol)
mcp==1.6.0                # MCP 协议支持

# 向量数据库（不变）
chromadb==1.5.1

# Web 框架 + SSE（不变）
fastapi==0.115.0
uvicorn==0.30.1
sse-starlette==2.1.0

# 配置 & 环境变量（不变）
pydantic==2.8.2
pydantic-settings==2.13.1
python-dotenv==1.0.1

# 工具 & 文档解析（不变）
numpy==1.26.4
httpx==0.27.0
aiofiles==23.2.1
beautifulsoup4==4.12.3
lxml==5.3.0
pypdf==5.1.0
python-docx==1.1.2
tiktoken==0.8.0           # 可选保留，如果还想用 OpenAI tokenizer 做 token 计数

python-multipart==0.0.12