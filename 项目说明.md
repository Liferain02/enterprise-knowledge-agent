# 实验室智能助手

面向计算机专业研究生实验室课题组的智能助手，支持实验室知识库检索、多 Agent 协作、长期记忆、流式对话、多用户认证与轻量权限控制。

当前项目已经重构为“实验室科研协作助手”，重点面向以下场景：

- 新生入组导览
- 课题资料与研究计划检索
- 论文笔记、组会纪要、实验记录管理
- 环境配置、集群使用、RDMA/分布式 NUMA 资料问答
- 公共资料、项目资料、负责人可见资料的分层访问
- 回答引用来源、反馈闭环与知识缺口追踪
- 科研项目空间、显式成员管理与结构化实验记录

## 核心能力

- 收敛后的 Agent 主链：Planner 单一路由、ACL 感知知识 RAG、按需 Operation/General Agent、统一最终输出
- RAG 检索链路：BM25 + 向量检索 + Rerank + Corrective RAG 评估
- 结构化来源展示：回答携带文档标题、片段、作者、时间、项目名
- 实验室资料入库：支持 `pdf`、`md`、`txt`、`docx`
- 异步入库队列：文件落盘、SHA-256 去重、状态追踪、失败重试与安全替换
- 文档元数据：`doc_type`、`author`、`project_name`、`research_direction`、`visibility`、`tags`
- 科研工作流：项目负责人、成员 ACL、方向、状态、环境、Git commit、数据集版本、实验指标、结论与后续动作
- 组会行动项：从 Markdown 纪要提取待办、负责人和截止日期，并在项目任务板推进状态
- 新生导览：支持“我刚加入实验室应该先看什么”类问题
- 反馈闭环：记录“有帮助 / 不准确 / 缺少资料”，并跟踪知识缺口的处理状态与解决说明
- 轻量权限：支持 `public / project / restricted`

## 知识库结构

默认实验室知识目录位于 `data/knowledge/`，当前已按少量类别收敛为：

- `overview/`：实验室方向总览
- `projects/`：研究计划、课题资料、实验模板
- `papers/`：论文 PDF
- `meetings/`：组会与例会纪要
- `operations/`：入组流程、考勤制度、环境配置、FAQ、资源使用说明

## 快速开始

已安装依赖并配置好 `config/.env` 时，直接执行：

```bash
./scripts/start.sh
```

停止项目：

```bash
./scripts/stop.sh
```

启动脚本会将前端绑定到 `0.0.0.0:3000`、后端绑定到 `0.0.0.0:8010`，PID 和日志保存在 Git 忽略的 `.run/` 目录。脚本只会停止由它自己启动并记录的当前项目进程。

首次安装仍需执行：

```bash
conda activate agent-demo
pip install -r requirements.txt
cp config/env.template config/.env
# 必须替换 ADMIN_PASSWORD、JWT_SECRET_KEY 和 DASHSCOPE_API_KEY
# 当前主模型：LLM_PROVIDER=qwen、DASHSCOPE_MODEL=qwen3.5-flash
npm --prefix frontend install
./scripts/start.sh
```

默认前端使用 `3000`，后端使用 `8010`，避免与其他项目的 `5173/5174/8000` 端口冲突。服务运行在远程服务器时，不能直接在自己电脑上打开 `localhost:3000`，需要使用 SSH 本地转发；完整命令和排错方法见[启动与远程访问指南](docs/启动与远程访问指南.md)。

## 重新导入实验室知识库

```bash
conda activate agent-demo
python scripts/reingest_lab_knowledge.py
```

如果需要一起导入论文 PDF：

```bash
python scripts/reingest_lab_knowledge.py --include-pdf
```

## 初始化科研空间样例

脚本会幂等写入 Distributed NUMA 与 RDMA 测试项目，重复执行不会创建重复数据：

```bash
conda activate agent-demo
python scripts/seed_research_workspace.py
```

## 初始化实验室成员

成员初始化脚本会在交互终端中为每个待创建账号隐藏询问并二次确认密码；密码不会写入仓库或打印到终端，已有账号会幂等跳过：

```bash
conda activate agent-demo
python scripts/add_employees.py
```

## 主要接口

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `POST /api/v1/chat`
- `POST /api/v1/chat/stream`
- `POST /api/v1/knowledge/add/file`
- `POST /api/v1/knowledge/search`
- `GET /api/v1/knowledge/documents`
- `GET /api/v1/knowledge/ingestion/jobs`
- `GET /api/v1/research/overview`
- `GET /api/v1/research/projects`
- `POST /api/v1/research/projects`
- `GET /api/v1/research/projects/{project_id}/experiments`
- `POST /api/v1/research/projects/{project_id}/experiments`
- `POST /api/v1/research/projects/{project_id}/tasks/extract`
- `PATCH /api/v1/research/tasks/{task_id}`
- `POST /api/v1/feedback`
- `GET /api/v1/feedback/stats`
- `GET /api/v1/feedback/issues`
- `PATCH /api/v1/feedback/issues/{feedback_id}`

## 当前定位

这个项目定位为“实验室内部科研知识与协作助手”。持续改进只按以下顺序投入：

1. 资料入库、检索、回答正文与来源可信。
2. 不准确和缺资料反馈能被处理。
3. 科研项目、实验和组会待办可持续记录。
4. 明确影响使用的性能、恢复和窄屏问题。

登录、MCP 和多 Agent 编排只是支撑层，除非阻断上述流程、导致越权或数据丢失，否则不作为优先开发对象。

## 项目文档

- [启动与远程访问指南](docs/启动与远程访问指南.md)
- [真实浏览器长期审查目标](docs/真实浏览器长期审查目标.md)
- [持续产品审查记录](docs/持续产品审查记录.md)
- [知识目录索引](data/knowledge/知识目录索引.md)
