# P4 目标：脱敏真实回答评测

## 1. 目的

选择性路由的规则评测已经通过，但规则正确不等于回答质量正确。本阶段使用少量已脱敏、已冻结的实验室问题，真实调用现有 Normal 和 Deep 链路，检查多角色协作是否带来稳定收益。

## 2. 样本

- 复杂样本：跨资料综合、时间/冲突判断、错误前提核验。
- 普通样本：单资料事实、概念解释、简单操作清单。
- 每类各 3 条，共 6 条；不复制真实用户会话，不写入知识库。
- 复杂样本来自现有冻结数据集，普通样本为固定对照问题。

## 3. 方案

1. `all_normal`：每条问题实际走普通链路。
2. `all_deep`：每条问题实际走固定 Deep Research 链路。
3. `selective_deep`：不额外调用模型，按现有 `is_complex_research_task` 选择 Normal 或 Deep 的实际结果。

选择性方案复用同一条实际运行结果，不重复调用第三次，避免浪费 API 和让比较失去可解释性。

## 4. 指标

- 关键词覆盖：是否包含样本预设的关键概念。
- 文档召回：是否找到了样本要求的参考资料。
- 引用覆盖与引用支持 proxy：自动回归指标，不冒充严格事实蕴含。
- 错误/成立前提判断：只对前提样本统计。
- ACL 泄漏：必须为 0。
- 延迟、输入/输出 Token、逻辑调用估计：仅用于相对比较。

人工评分仍然是最终质量依据；本阶段自动指标只决定是否值得进入下一轮人工盲评，不直接宣称 Deep 优于 Normal。

## 5. 停止规则

如果 Deep 只增加延迟和调用成本，而在复杂问题的文档召回、引用支持或前提判断上没有稳定收益，则停止自动路由改造，继续使用显式 Deep 模式。不要为了让某个指标变好而扩充 Agent、修改提示词或引入新基础设施。

## 6. 运行

```bash
conda activate agent-demo
python -m tests.eval.脱敏真实回答评测 --limit 6 --write
```

评测支持 `--checkpoint` 断点恢复。输出的 JSON、报告和断点文件属于本地评测产物，不提交真实回答内容到代码仓库。
