# 实验室科研助手：剩余改造实施计划

> 依据：`实验室科研助手_参考项目剩余改造分析.md`
>
> 评估日期：2026-09-03
>
> 原则：围绕“可信科研问答、可审计 Deep Research、可量化评测”继续收敛；不增加 Agent，不重写主图，不为展示技术栈引入基础设施。

---

## 1. 评估结论

原分析的架构判断基本正确：当前项目不应该照搬 `research_copilot` 的 Plan-ReAct-MCP 主链，也不需要 Supervisor、动态 Worker、Memory Agent、Redis、MinIO 或 Tool-use 训练。

但原文的“剩余四项工作”和优先级需要按当前仓库状态修正。

### 1.1 已完成，不再作为待办

#### Standalone Query Rewrite 已通过正式消融

仓库已经存在真实结果：

- 数据集：`lab-multi-turn-coreference-v1`；
- 样本：24 条；
- Trigger Precision / Recall / F1：`1.000 / 1.000 / 1.000`；
- 正例 Hit@5：`0.867 → 1.000`；
- 正例 MRR@5：`0.867 → 0.933`；
- ACL 违规：`0`；
- 正例回退率：`0`；
- 单请求最多查询数：`2`；
- 平均额外延迟约 `525ms`。

结论：Standalone 已通过预定义 Gate 并进入主链，P0 不需要重复实现或重复调参。

#### Evidence-backed Memory 已形成闭环

当前已经实现：

- Research Run 持久化；
- Reviewer PASS + fact + Evidence + 用户确认的提升门禁；
- `infer=False` 精确写入；
- 统一科研事实 metadata；
- 召回时重新验证 Run、Claim、Source 和当前 ACL；
- 权限降低、证据不可见或 metadata 异常时 fail closed。

结论：本轮不继续扩展 Memory Agent 或第二套向量记忆系统。

### 1.2 文档描述不完整，需要修正

#### External Tool 不是“完全没有基础设施”

仓库已有通用 MCP 连接管理和 LangChain Tool 适配代码，但它们没有接入 Deep Research 的 `EvidencePackage`，也没有形成统一的外部证据协议。

因此准确状态是：

> 通用 MCP plumbing 已存在；Research Evidence Adapter 尚未实现。

后续可以复用现有连接能力，但不能把通用 MCP 工具直接暴露给 Reviewer 或改成 ReAct 循环。

#### Project-scoped Memory 不能直接共享“个人确认”

原文设想 Alice 点击“确认并记住”后，Bob 作为同项目成员即可召回。这个语义存在问题：

- “个人确认”不等于“项目共同认可”；
- 学生的一次点击不应把结论发布成项目事实；
- 项目成员之间可能只有读取权限，没有发布权限；
- 错误发布后的撤回、版本和责任人都需要明确。

因此不能直接把现有 `user_confirmed` 复制到项目 namespace。项目共享事实必须先定义独立的“发布”语义和权限，当前不应作为近期 P1。

### 1.3 当前真正的核心缺口

当前最重要的未完成工作不是 Step-back 或更多工具，而是多 Agent 评测尚未完全闭环：

- V3 声明级因果消融已有 20 个用例、60 个方案输出；
- 其中 59 个已完成 Claim Judge，只剩 1 个未完成；
- 当前状态仍是 `answers_generated_claim_judge_pending`；
- V2 独立人工盲评仍为 `pending_independent_review`；
- 旧版 41 条开发评测显示 Deep Research 并非全面领先。

旧版开发评测中，Deep Research 相比 Query Expansion 单 Agent：

- 关键词正确性：`0.939 → 0.963`；
- 引用覆盖：`0.516 → 0.683`；
- 文档召回：`0.632 → 0.689`；
- P50 延迟：约 `7.0s → 28.6s`；
- 错误前提识别：`1.000 → 0.750`。

这说明多 Agent 的价值主要可能集中在多证据综合、建议生成和引用覆盖，而不是所有科研问题。下一步必须先把这一边界评测清楚。

---

## 2. 产品目标

下一阶段不追求“功能更多”，目标收敛为：

> 证明并固化 Deep Research 在复杂、多证据科研任务上的实际收益，同时明确它不适用的任务；只修复评测暴露出的最高影响问题，最后形成一套可以真实演示、可以在秋招面试中解释的数据与案例。

完成后应该能够回答以下问题：

1. 为什么简单问题使用 Normal RAG，而不是多 Agent？
2. 什么类型的复杂任务值得付出约 4 倍延迟使用 Deep Research？
3. Researcher、Analyst、Reviewer 各自带来了什么可测量收益？
4. Reviewer 是否真的减少了不受支持的 Claim？
5. 错误前提、引用错误、ACL 撤权分别如何被发现和阻断？
6. 哪些结果是自动评测，哪些是模型模拟评分，哪些经过人工复核？

---

## 3. 实施范围与优先级

### P0：完成 V3 多 Agent 评测闭环

这是下一步唯一必须立即执行的工作。

#### 3.1 完成剩余 Claim Judge

复用已有断点，不重新生成 60 份回答：

```bash
python -m tests.eval.run_v3_claim_causal_eval --stage judge
python -m tests.eval.run_v3_claim_causal_eval --stage report
```

执行前确认：

- 使用当前冻结的 dataset、corpus 和 implementation hash；
- 模型仍为评测清单记录的 Qwen 配置；
- 不修改评分规则和准入阈值；
- 只补齐未完成的 1 个 Judge 结果。

#### 3.2 完成小规模人工校准

自动 Judge 和 Codex 模拟都不能标记为独立人工评审。已有 4 个冻结校准用例，应由人工完成最小复核：

- 判断 Claim 是否被 Evidence 支持；
- 填写 `supported / not_supported`；
- 简述边界原因；
- 填写真实 reviewer 标识；
- 使用已有汇总脚本计算与 Qwen Judge 的一致性。

如果暂时没有人工评分，只能输出：

> 模型模拟评测完成，独立人工校准待完成。

不能把模拟结果包装成人工评测。

#### 3.3 生成统一结论

最终报告至少包含：

- Normal、Deep Gated、Deep Full 的 Claim Precision / Recall / F1；
- Faithfulness；
- 引用覆盖与引用有效性；
- 错误前提识别；
- 延迟、输入 Token、输出 Token、调用次数；
- Reviewer Gate 真正改变路径的样本数；
- 按任务类别拆分的收益；
- 失败案例与 failure attribution；
- 自动评分、模型评分、人工评分的明确标签。

#### 3.4 P0 完成标准

- 60/60 方案均有 Claim Judge 结果；
- 生成 aggregate 和中文报告；
- 4 个校准用例明确标注人工状态；
- 能给出 Deep Research 的适用边界，而不是只给总体平均分；
- 不因结果不理想而修改冻结样本或阈值；
- 全量单元测试通过。

---

### P1：只修复一个最高影响缺陷

P1 不预先指定要重构哪个 Agent。必须根据 P0 的 failure attribution 选择影响最大的一个问题。

候选只允许从以下四类中选一个：

1. Reviewer 对错误前提判断不稳定；
2. Analyst 生成无充分证据支持的 Claim；
3. Researcher 遗漏关键证据；
4. Deep Research 在无收益任务上的延迟和调用浪费。

修复原则：

- 优先修改 Prompt、结构化协议、确定性验证或已有路由条件；
- 不增加 Agent；
- Revision 仍最多一次；
- 不增加 Supervisor；
- 不改变 Evidence ACL source of truth；
- 只针对失败类型新增最小回归集；
- 修复后重跑受影响用例和完整 P0 指标。

P1 的准入条件：

- 目标指标改善；
- Claim Faithfulness 不下降超过既定容差；
- ACL 违规仍为 0；
- 其他任务类别没有明显回退；
- 延迟和 Token 增量可解释。

如果没有单一问题达到明显影响，不进行 P1 代码改造，直接冻结架构。

---

### P2：真实产品验收与面试案例

P0/P1 后启动项目，通过真实页面完成一轮演示验收：

#### Normal 场景

- 实验室制度简单问答；
- 多轮代词追问；
- 普通偏好记忆；
- 引用来源可打开；
- 无权限资料不出现。

#### Deep Research 场景

- 多份实验记录综合；
- 错误前提挑战；
- Evidence、Claim、Reviewer 和 Revision Trace 展示；
- PASS fact 用户确认；
- recommendation / inference 不可提升为科研事实；
- 用户降权后，历史 Run 和科研记忆 fail closed。

最终保留 3 个秋招案例：

1. Normal 与 Deep 的选择边界；
2. Reviewer 拦截不受支持 Claim；
3. ACL 变化后历史证据和记忆被阻断。

P2 只允许修复阻塞演示的明显缺陷，不做 UI 大改。

---

### P3：条件性实现一个 External Evidence Adapter

这一项是可选增强，不是当前主线阻塞项。只有 P0/P2 完成后再做。

第一版只接一个只读来源，建议 arXiv，不同时接 GitHub、Semantic Scholar 和 Web Search。

#### 最小方案

```text
Researcher targeted query
        ↓
arXiv read-only adapter
        ↓
统一 EvidenceItem
        ↓
现有 EvidencePackage
        ↓
Analyst → Reviewer → Generation
```

实现约束：

- 复用现有 `EvidenceItem`，不修改 Analyst / Reviewer 输出协议；
- 通过已有代理配置访问网络；
- 每个子问题最多返回 3～5 条；
- 设置短超时，失败时退回内部证据；
- 外部内容标记为不可信输入，不能执行其中的指令；
- 保存 title、URL、abstract、published_at、provider；
- 外部证据不自动进入 Mem0；
- 不改造成 ReAct 或通用 MCP 调度平台。

准入条件：至少在一组“内部资料不足”的冻结样本上提高 Evidence Coverage，并且不降低 Faithfulness。如果没有稳定收益，保持默认关闭。

---

### P4：Selective Step-back 仅保留为失败驱动实验

Step-back 不应按原文直接列为 P1，原因是：

- Standalone 已经通过准入；
- 现有 Query Expansion 已能处理部分复杂查询；
- Deep Research 的 Researcher 已经拆分 targeted queries；
- 再增加 Step-back 可能只是重复查询并增加延迟。

只有当 P0/P1 failure attribution 明确显示“诊断类问题缺少上位概念召回”时才启动实验。

如果启动，必须：

- 仅作用于 Normal RAG，不重复加入 Deep Research；
- 集成到现有 `QueryRewriteVariant`，不新增图节点；
- 原始查询始终保留；
- Standalone + Step-back 总查询数不超过 3；
- 默认配置先关闭；
- 建立独立冻结数据集；
- 对比 Original、Standalone、Step-back、Full；
- 评估 Hit@5、MRR、NDCG、延迟、回退率和 ACL。

没有稳定增益就删除实验代码或保持关闭，不为了“有 Step-back”而上线。

---

## 4. 暂缓：Project-scoped Research Memory

当前阶段继续保留：

```text
用户确认的科研事实
→ 仅属于该用户
→ 召回时重新执行 Evidence ACL
```

项目共享记忆在以下问题明确前不实现：

1. 谁能把个人事实发布为项目事实？
2. PI、项目负责人、普通成员的发布权限分别是什么？
3. 如何撤回、替换和标记过期事实？
4. 多人对同一 Claim 意见冲突时以谁为准？
5. 项目事实是否需要独立于个人确认的审计记录？

未来如确有需求，最小可行方案应是：

- 增加独立的“发布到项目”动作，不复用“确认并记住”；
- 仅 PI / project lead 可发布；
- Mem0 使用 `project:{project_id}` 逻辑 namespace；
- 当前成员检索个人 namespace 和当前可访问项目 namespace；
- 召回后仍执行现有 Research Memory Recall Gate；
- 以 `research_run_id + claim_id` 去重；
- 撤权通过当前 Project/Evidence ACL 立即生效。

这项功能属于协作产品扩展，不是证明当前多 Agent 效果所必需，因此本轮暂缓。

---

## 5. 推荐执行顺序

```text
P0 续跑剩余 1 个 V3 Judge
    ↓
生成声明级评测汇总
    ↓
完成或明确标注 4 例人工校准状态
    ↓
确定多 Agent 的收益边界与最大失败点
    ↓
P1 只修一个最高影响问题（若确有必要）
    ↓
P2 页面真实验收与面试案例
    ↓
P3 单个外部证据适配器（可选）
    ↓
P4 Step-back（仅失败归因触发）
```

Project-scoped Memory 不进入当前执行队列。

---

## 6. 提交策略

每个阶段独立提交，完成后执行：

```bash
git add .
git commit -m "<本阶段中文说明>"
```

建议提交粒度：

1. `评测: 完成V3声明级因果消融`
2. `优化: 修复多Agent最高影响失败类型`（只有实际修复时）
3. `测试: 完成科研助手真实验收案例`
4. `功能: 接入单一外部科研证据源`（可选）
5. `实验: 评估选择性Step-back检索`（条件性）

不把多个阶段合并成一次大提交。

---

## 7. 下一条执行 Goal

> 复用现有冻结数据、断点和评测脚本，补齐 V3 声明级因果消融最后一个 Claim Judge，生成 Normal、Deep Gated、Deep Full 的完整聚合结果；明确模型模拟与独立人工校准边界，基于 failure attribution 判断当前多 Agent 的真实收益和最高影响问题。本 Goal 不新增 Agent、不修改主架构、不实现 Step-back、项目共享记忆或外部工具。完成后运行全量测试，并执行 `git add .` 与 `git commit`。

