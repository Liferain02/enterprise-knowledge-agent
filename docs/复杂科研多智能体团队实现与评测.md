# 复杂科研多智能体团队实现与评测

本文说明固定 Researcher → Analyst → Reviewer 团队的产品边界、状态协议、代码实现、安全约束、一次修订机制和 A/B/C 评测方法。36 条全量评测表明它不能替代单 Agent 复杂问答；生产仅在“需要多证据核验错误或绝对化前提”的窄场景启用。

## 1. 设计目标

要验证的命题只有一个：

> 对需要多来源证据、事实/推断分离和独立复核的复杂科研任务，固定专业分工能否比单 Agent 更可信。

不以“运行更多 Agent”为目标。若评测不能证明质量收益足以覆盖延迟和 API 成本，应简化或删除该团队。

## 2. 明确不做什么

本实现没有增加：

- Supervisor；
- 动态创建 Agent；
- 用户自定义角色；
- Agent 自由协商；
- Agent 间长期 Memory；
- Blackboard、Mailbox 或消息队列；
- 动态团队拓扑；
- Redis、Kafka、知识图谱或 GraphRAG；
- 新 Agent 框架；
- 为每个 RAG 步骤包装一个 Agent。

团队复用现有 LangGraph、Planner、ACL、Hybrid Retrieval、Qwen Rerank、CRAG、Chroma 和 Finalizer。

## 3. 使用边界

### 3.1 继续走原单 Agent RAG

以下问题不会进入团队：

- “实验室组会多久一次？”；
- “有哪些 RDMA 实验要求？”；
- “对比 RDMA 和 TCP 的差异”；
- 普通制度、流程、项目事实；
- 单份或少量资料即可回答的问题。

其中对比、列举等一般复杂知识问题仍使用 Query Expansion + Retrieval → Generation。

### 3.2 生产环境进入固定团队

生产必须同时满足两个条件：

1. `is_complex_research_task()` 判断为需要多证据综合的科研任务；
2. 问题明确要求验证“所有、必然、完全相同、都无需”等错误或绝对化前提。

最终生产门是 `should_use_research_team()`。`is_complex_research_task()` 仍用于识别 36 条宽口径评测集，使 A/B/C 能覆盖跨项目、多证据、时间冲突、建议和错误前提五类任务；它不再等于生产路由。这一收缩来自全量数据，而不是主观偏好：C 的总体正确性低于 A/B，只有错误前提分组同时取得关键词正确性、引用质量和冲突处理收益。

跨项目比较、多证据总结、时间演化和研究建议若不包含待核验的绝对化前提，继续使用 Query Expansion + 单 Agent。

## 4. 生产图

```text
maybe_summarize
  → retrieve_mem0_memories
  → planner（唯一入口）
      ├─ 普通知识 → retrieval_agent → generation_agent ──────────────┐
      ├─ 错误前提科研核验 → research_agent → analyst_agent → reviewer_agent
      │                                      ├─ PASS ────────────────┤
      │                                      └─ REVISE/NEED_MORE     │
      │                                           ↓                  │
      │                                    research_revision         │
      │                                           ↓                  │
      │                               research_team_finalizer ───────┤
      ├─ 工具 → operation_agent ─────────────────────────────────────┤
      └─ 通用 → general_agent ───────────────────────────────────────┤
                                                                  ↓
                                                        finalize_response → END
```

`research_revision` 没有回到 Reviewer 的边，因此图结构上不可能形成循环。一次修订后强制进入 `research_team_finalizer`。

## 5. 最小共享状态

`ResearchTaskState` 只包含：

| 字段 | 内容 |
|---|---|
| `research_question` | 原始复杂科研问题 |
| `evidence_package` | Researcher 输出的结构化证据 |
| `analysis_report` | Analyst 的 Claim 与草稿 |
| `review_report` | Reviewer 的结构化复核 |
| `research_revision_count` | 0 或 1 |
| `research_team_metrics` | LLM/检索调用、token 和耗时 |

它嵌入主 `AgentState`，不另建团队数据库、长期记忆或消息系统。节点通过结构化对象交换信息，不以自由自然语言对话作为主要协议。

## 6. EvidencePackage

Pydantic 模型定义在 `research_team.py`：

```text
EvidencePackage
  original_question: str
  subquestions: list[str]          # 最多 4 个
  evidences: list[EvidenceItem]    # 最多 12 条
  missing_evidence: list[str]
  conflicts: list[str]
  acl_checked: bool

EvidenceItem
  source_id: str                   # S1、S2……
  subquestion: str
  title: str
  source: str                      # 只保留安全文件名
  excerpt: str                     # 最多 800 字符
  score: float
  metadata: dict                   # 白名单元数据
  time_info: str
  conflict_warnings: list[str]
```

绝对服务器路径不会进入协议。metadata 只保留 title、类型、作者、项目、研究方向、visibility、密级、版本和时间等必要字段。

## 7. Research Agent

入口是 `research_agent_node()`。

### 7.1 有限问题拆分

Researcher 使用结构化 `SubquestionPlan` 生成 2～4 个可检索子问题。模型结构化输出失败或返回空列表时，`_fallback_subquestions()` 生成有限确定性计划，不会创建新角色。

最大值常量：

- `MAX_SUBQUESTIONS = 4`；
- `MAX_TARGETED_QUERIES = 2`；
- `MAX_EVIDENCES = 12`；
- `MAX_EVIDENCE_CHARS = 800`。

12 条 × 800 字符是实测后的上下文上限，用于避免 Analyst 在宽检索结果上超过模型输出/上下文预算；它不是新的检索基础设施。

### 7.2 复用现有检索

每个子问题调用同一个 `CorrectiveRAGPipeline.retrieve()`：

```text
ACL-aware Hybrid Retrieval
  → Qwen Rerank
  → CRAG grade
  → 必要时管线自身纠错
```

团队没有专用向量库或绕过 ACL 的快速通道。

### 7.3 ACL 二次复核

即使 Pipeline 已过滤，Researcher 仍对每个返回文档调用 `check_doc_access()`。没有用户上下文时按 `UserContext.anonymous()` 的 public 权限处理，而不是全库访问。

被拒绝的文档不会进入 EvidencePackage、Analyst prompt、最终来源卡片或评测结果。

### 7.4 聚合与去重

证据以 source + 正文前缀去重，保留分数更高项，再按相关性排序并编号 S1～Sn。来源文档由 `_documents_from_package()` 重建，确保修订后来源卡片顺序仍与 S 编号一致。

### 7.5 冲突和缺口

没有结果或子查询异常会进入 `missing_evidence`，不会使整个团队无限重试。多个文档进入现有 conflict detector，冲突写入 package。

Researcher 不生成最终答案，也不修改科研数据。

## 8. Analyst Agent

入口是 `analyst_agent_node()`。它只读取 EvidencePackage。

### 8.1 Claim 协议

```text
Claim
  claim_id: str
  text: str
  claim_type: fact | comparison | inference | recommendation
  source_ids: list[str]
  confidence: high | medium | low

AnalysisReport
  claims: list[Claim]
  comparison: str
  limitations: list[str]
  draft_answer: str                # 当前要求留空，由 Finalizer 确定性渲染
```

事实、比较、模型推断和研究建议必须显式区分。重要 Claim 绑定 S 编号。当前 Prompt 要求 `draft_answer` 留空，最终文本统一由 Finalizer 从 Claim 确定性渲染，避免自由草稿绕过声明级引用。

### 8.2 安全边界

Prompt 明确声明 EvidencePackage 是不可信数据：文档中的指令不得改变系统规则。Analyst 不检索、不调用工具、不创建 Agent，也不能用外部常识补实验室内部事实。

### 8.3 结构化兼容

Qwen 偶尔只填 `draft_answer` 而不填 `claims`。`_normalize_analysis()` 会从带 S 引用的草稿中恢复 Claim，并根据“推断、可能、暗示、建议”等词区分类型；它不会为没有引用的句子凭空创建证据。

## 9. Reviewer Agent

入口是 `reviewer_agent_node()`。它只允许三个决定：

- `PASS`；
- `REVISE`；
- `NEED_MORE_EVIDENCE`。

任何其他值都会被 Pydantic 拒绝。

### 9.1 ReviewReport

```text
ReviewReport
  decision
  items: list[ReviewItem]
  false_premise_detected
  conflict_handled
  acl_verified
  overall_instruction
  targeted_queries            # 最多 2 条

ReviewItem
  claim
  source_ids
  supported
  issue_type
  revision_instruction
```

### 9.2 确定性护栏

LLM Reviewer 之外还有程序检查：

- source_id 不存在 → `invalid_source`；
- Claim 没有引用 → `citation_gap`；
- package 未确认 ACL → `acl`；
- 用户要求验证“所有、必然、完全相同、都无需”等绝对化前提 → 显式 `false_premise` 核验。

因此 Reviewer 即使误返回 PASS，也不能放过无效引用或明显需要核验的绝对化前提。

Reviewer 不生成最终答案、不直接检索，也不能启动其他 Agent。

## 10. 一次受限修订

`route_after_reviewer()` 的规则：

```text
PASS → research_team_finalizer
REVISE/NEED_MORE_EVIDENCE 且 revision_count=0 → research_revision
任意决定且 revision_count>=1 → research_team_finalizer
```

`research_revision_node()`：

- REVISE：将 ReviewReport 交给 Analyst 修订一次；
- NEED_MORE_EVIDENCE：最多执行 2 个 targeted query，合并去重后让 Analyst 修订一次；
- 修订完成写 `research_revision_count=1`；
- 不再返回 Reviewer。

这意味着最多一次补检索或修订，不存在依赖模型“自觉停止”的循环上限。

## 11. 团队终态

`research_team_finalizer_node()` 不再调用模型。它：

- 无证据时确定性拒答；
- 无论 PASS 或修订，都按 Claim 类型渲染“资料事实/资料比较/模型推断/研究建议”；
- 保留 `[Sx]` 引用；
- 错误前提时在顶部显式提示；
- 存在冲突时追加冲突说明；
- 标记 `used_agent=research_team`。

之后仍进入全局 `finalize_response`，因此产品只保存一个最终 AIMessage。

## 12. SSE 与前端

团队的结构化 LLM 调用不带 `user_visible_answer` 标签，内部 JSON 不会流到浏览器。ChatService 只展示阶段状态：

- 正在拆分问题并收集多来源科研证据；
- 正在区分资料事实、模型推断与研究建议；
- 正在独立复核引用、错误前提和资料冲突；
- 正在按复核意见完成一次受限修订。

团队终态完成后，ChatService 从 checkpoint 取得 `final_answer` 并一次性补发，同时发送与 S 编号同序的来源卡片和版本信息。

## 13. 成本与可观测性

`research_team_metrics` 累积：

- `llm_calls`；
- `retrieval_calls`；
- `input_tokens` / `output_tokens`；
- `estimated_prompt_chars`；
- `elapsed_ms`。

token 取 OpenAI-compatible 响应的 `usage_metadata`。CRAG 内部每篇文档 grader 的 token 并不全部暴露给团队状态，因此评测报告同时给出 logical API call estimate，并明确它不是账单数字。

## 14. 评测集

`tests/eval/complex_research_dataset.py` 包含 36 条复杂科研问题，满足 30～50 条目标。分类：

| 类别 | 数量 | 重点 |
|---|---:|---|
| `cross_scope` | 8 | 跨项目、方向或论文比较 |
| `multi_evidence` | 8 | 论文、实验、纪要、制度共同支撑 |
| `temporal_conflict` | 8 | 时间演化和冲突 |
| `recommendation` | 8 | 基于证据的下一步建议 |
| `false_premise` | 4 | 绝对化或错误前提识别 |

每条包含 relevant_doc_ids、expected_keywords、最少来源数、是否要求错误前提/冲突处理等标注。full dry-run 会检查全部 36 条都能被团队路由识别。

## 15. A/B/C 评测

运行脚本：`tests/eval/run_research_team_eval.py`。

三种方案：

- A：Hybrid/CRAG + 单 Agent，不主动 Query Expansion；
- B：Hybrid/CRAG + Query Expansion + 单 Agent；
- C：Researcher + Analyst + Reviewer。

指标：

- 关键词正确性代理指标；
- 声明级引用覆盖率；
- 引用支持率；
- 错误前提识别率；
- 冲突处理正确率；
- ACL 泄漏数；
- 目标文档 recall；
- P50/P95 延迟；
- 可观测 token；
- 逻辑 API 调用估计。

命令：

```bash
# 只校验 36 条数据和路由，不调用模型
python tests/eval/run_research_team_eval.py --dataset full --dry-run

# 每类一条，共 5 条，执行 A/B/C
python tests/eval/run_research_team_eval.py --dataset quick

# 全量 36 条
python tests/eval/run_research_team_eval.py --dataset full

# 单案例调试
python tests/eval/run_research_team_eval.py --case-id C33 --variants C
```

输出默认是 `data/复杂科研评测结果.json` 和 `data/复杂科研评测报告.md`。

## 16. 全量 36 条实测与生产决策

2026-08-12，在当前 Qwen、知识库和代理环境上完成 36 条 A/B/C 全量运行：

| 方案 | 成功 | 关键词正确性 | 引用覆盖 | 引用支持 | 错误前提 | 冲突处理 | ACL 泄漏 | P50 | P95 | 估计调用 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A Hybrid 单 Agent | 36/36 | 0.949 | 0.181 | 0.333 | 1.000 | 0.972 | 0 | 6.2s | 9.5s | 1.97 |
| B Expansion 单 Agent | 36/36 | 0.935 | 0.182 | 0.333 | 1.000 | 0.972 | 0 | 7.3s | 10.6s | 1.97 |
| C 固定团队 | 36/36 | 0.912 | 0.475 | 0.806 | 1.000 | 0.972 | 0 | 16.6s | 20.9s | 6.92 |

结论不是“C 全面更好”：

- C 的引用覆盖比 B 高 0.293，引用支持高 0.473，ACL 泄漏保持 0；
- C 的总体关键词正确性比 B 低 0.023，比 A 低 0.037；
- C 的 P50 是 B 的约 2.27 倍，逻辑调用是 B 的约 3.51 倍；
- 错误前提分组中，C 的关键词正确性为 0.875，B 为 0.500；引用覆盖 0.385 对 0.125，引用支持 0.688 对 0.250，冲突处理 1.000 对 0.750；
- 其他四类虽提升引用质量，但没有同时保持总体正确性，因此不进入生产团队。

由此执行“有收益才保留”的约束：固定团队代码保留，但生产门收缩到复杂错误/绝对化前提核验；普通复杂科研问题回到 B 类路径。原始逐条结果见 `data/复杂科研评测结果.json`，可读汇总见 `data/复杂科研评测报告.md`。

## 17. 保留或删除判据

建议只有同时满足以下条件才长期保留：

1. ACL 泄漏始终为 0；
2. 复杂任务引用覆盖/支持率相对 B 有稳定提升；
3. 错误前提和冲突处理不低于 B；
4. 正确性不下降；
5. 用户能接受约 20～30 秒的复杂分析延迟；
6. 真实科研问题人工复核确认建议确由证据支撑。

本轮全量结果没有证明 C 在宽口径任务上有净收益，因此已经缩小生产路由，没有增加第四个 Agent、第二轮循环或新基础设施。未来只有在新数据集上同时满足上述质量门，才可扩大 `should_use_research_team()`；不得仅修改路由规则而跳过 A/B/C 复测。
