# 实验室科研助手：V3 声明级评测阶段报告

> 评测日期：2026-09-03
>
> 状态：Qwen Claim Judge 已完成，独立人工校准待完成
>
> 性质：Development Eval，不是 Blind Holdout

## 1. 本轮完成内容

- 复用冻结的 20 个评测问题和 60 个已有方案回答；
- 补齐唯一缺失的 `V320 / deep_full` Claim Judge；
- 60/60 个方案均完成三轴声明级评分；
- 生成 Normal、Deep Gated、Deep Full 聚合结果；
- 增加按任务类别的 F1 与 Faithfulness 对比；
- 将人工校准从 397 个候选判断收敛为 36 个确定性分层样本；
- 保留独立人工校准边界，没有把模型评分伪装成人工评分。

## 2. 冻结协议处理

首次续跑时，评测脚本发现 V3 生成完成后 `research_team.py` 已经变化，因此拒绝混用结果。

本轮没有关闭冻结检查，而是把检查拆成两个范围：

### Generation

执行回答生成时继续要求：

- 数据集哈希一致；
- corpus 快照一致；
- 完整生成实现快照一致；
- 方案集合一致。

任意一项变化都拒绝续跑。

### Judge

Judge 只消费已经落盘的 answer 和 retrieved context，因此要求：

- 结果绑定原冻结清单；
- 数据集哈希一致；
- Claim Judge 实现哈希一致；
- 不重新执行已经变化的生成代码；
- 在结果中记录冻结后实现漂移。

这样既没有用新实现重算单个旧样本，也没有因与评分无关的生成代码变化永久阻塞最后一次 Judge。

## 3. 总体结果

| 方案 | Claim Precision | Claim Recall | Claim F1 | Faithfulness | P50 / P95 延迟 | 平均输入 / 输出 Token | 逻辑调用 |
|---|---:|---:|---:|---:|---:|---:|---:|
| Normal | 0.417 | 0.651 | **0.491** | 0.760 | 8.3s / 17.7s | 2131 / 367 | 2.00 |
| Deep Gated | **0.433** | 0.662 | 0.472 | 0.737 | 35.7s / 51.8s | 11730 / 1883 | 6.80 |
| Deep Full | 0.360 | **0.734** | 0.466 | **0.781** | 42.1s / 57.3s | 13964 / 2310 | 7.40 |

当前自动评测不支持“多 Agent 全面优于 Normal RAG”的结论：

- Deep Gated 的 Precision 略高，但 F1 和 Faithfulness 没有超过 Normal；
- Deep Full 的 Recall 和 Faithfulness 最高，但 Precision 最低；
- Deep 延迟约为 Normal 的 4～5 倍；
- Deep 的 Token 与调用次数明显增加。

因此 Deep Research 继续保持显式模式是合理的，不能改成默认路径。

## 4. 按任务类别结果

| 类别 | Normal F1 | Gated F1 | Full F1 | Normal Faithfulness | Gated Faithfulness | Full Faithfulness |
|---|---:|---:|---:|---:|---:|---:|
| 证据与复现 | **0.430** | 0.395 | 0.360 | 0.618 | **0.708** | 0.700 |
| 实验设计 | 0.354 | 0.316 | **0.413** | 0.613 | 0.658 | **0.783** |
| 前提判断 | **0.608** | 0.543 | 0.494 | **0.886** | 0.672 | 0.764 |
| 系统实现 | 0.573 | **0.636** | 0.597 | **0.923** | 0.908 | 0.875 |

目前可观察到的适用边界：

- Deep Gated 在系统实现类任务的 Claim F1 更好；
- Deep Full 在实验设计类任务的 F1 与 Faithfulness 更好；
- 前提判断类任务上 Normal 明显更好；
- 证据复现类任务中 Deep 提高 Faithfulness，但没有提高 Claim F1。

这些结论仍需人工校准确认，不能直接写成最终效果声明。

## 5. Actionability Gate 因果结果

Actionability Gate 在 11/20 个问题上真正改变了路径。

相对 Deep Full，Deep Gated：

- Claim F1：`+0.006`；
- Faithfulness：`-0.044`；
- 平均延迟：降低 `14.6%`；
- 输入 Token：降低 `16.0%`；
- 输出 Token：降低 `18.5%`。

按照评测前已经固定的准入条件：

- 成本预门禁：通过；
- 质量预门禁：失败。

因此现在不能为了节省成本就宣布 Actionability Gate 有效。

## 6. 人工校准任务优化

原生成逻辑会把 4 个校准 case 的所有抽取 Claim、三个方案和三个评分轴全部交给人工，共 397 个标签，实际不可执行。

本轮改成：

```text
4 个冻结 case
× 3 个方案
× 3 个评分轴
× 每层 1 个确定性样本
= 36 个标签
```

抽样方式为每个分层内选择 `sha256(task_id)` 最小的项目，具备以下性质：

- 不读取 Qwen verdict；
- 不根据结果挑样；
- 重复生成得到相同任务；
- 覆盖全部 case、方案和评分轴；
- 已开始填写后脚本拒绝覆盖。

待填写文件：

```text
data/深度研究V3人工校准评分.json
```

必须由未查看 Qwen verdict 的真实评审者填写：

- `reviewer_id`；
- `independence_attestation = true`；
- 36 个 `human_verdict`；
- 对应的 `human_reason`。

Codex 或其他模型可以模拟评分，但只能标记为模型模拟，不能替代这里的独立人工校准。

## 7. 当前决策

本轮不修改 Researcher、Analyst、Reviewer 或 Revision：

1. 自动 Judge 尚未完成人工校准；
2. 当前质量预门禁失败；
3. 多 Agent 的优势只在部分任务类别出现；
4. 此时继续调 Prompt 容易针对开发集过拟合。

人工校准后的决策规则：

- Judge 校准通过且质量门禁仍失败：回退 Actionability Gate；
- Judge 校准失败：不依据当前自动分数修改 Agent，先修正评测器；
- Judge 与质量门禁均通过：保留 Gate，再做真实页面验收；
- 无论哪种结果，Deep Research 都不自动替代 Normal RAG。

## 8. 下一步

当前只需要完成 36 个独立人工标签，然后运行：

```bash
python -m tests.eval.summarize_v3_human_calibration
```

在人工结果出来前，不实现 Step-back、项目共享记忆、更多 Agent 或外部工具。

