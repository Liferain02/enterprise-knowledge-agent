# 实验室科研助手：P5/P6 评估与执行计划

## 1. 先纠正编号

仓库中存在两套历史编号：

- `goal.md` 的 P5/P6：基于评测优化 Agent/提示词；真人评分与秋招材料。
- `实验室科研助手_知识演化与评测驱动优化任务书.md` 的 P5/P6：外部证据适配器；条件性关系图。

两套编号描述的是不同版本的路线，不能继续混用。以后以本文件和
`实验室科研助手_剩余改造实施计划.md` 为准：

| 阶段 | 唯一含义 | 当前状态 |
|---|---|---|
| P0 | V3 声明级评测、模拟校准和真实产品验收收尾 | 已完成自动评测；真人校准待实验室成员填写 |
| P5 | 根据失败归因只修一个最高影响缺陷 | 本 Goal 执行中 |
| P6 | 独立真人校准、秋招案例和结果表达 | 等待真人标签，可先准备材料 |
| 外部证据 | 单一只读科研来源适配器 | 可选，P5/P6 后再评估 |
| 关系图 | 普通数据库关系的条件性增强 | 暂缓 |

## 2. 项目重点判断

本项目的重点不是登录、Agent 数量或基础设施，而是实验室科研场景下的三件事：

1. 普通问题低延迟、可引用地回答；
2. 复杂多证据问题用显式 Deep Research，输出可追溯 Claim、Evidence、Reviewer 和 Revision Trace；
3. 证据、ACL、记忆和项目知识形成可审计闭环。

当前架构已经有固定 Researcher → Analyst → Reviewer → 一次 Revision → Generation 主链，
不需要照搬 `research_copilot` 的 Plan-ReAct-MCP 主链，也不需要新增 Agent。

## 3. 现有评测说明了什么

### 3.1 V3 声明级评测（20 条问题、60 份方案输出）

| 方案 | Claim F1 | Faithfulness | P50 | 结论 |
|---|---:|---:|---:|---|
| Normal | 0.491 | 0.760 | 8.3s | 总体 F1 和前提判断更稳 |
| Deep Gated | 0.472 | 0.737 | 35.7s | 节省成本，但质量门槛失败 |
| Deep Full | 0.466 | 0.781 | 42.1s | Recall/Faithfulness 较高，成本明显更高 |

任务拆分显示：Deep 的收益主要在实验设计和部分系统实现，不是所有科研问题都值得使用。
前提判断上 Normal 更好，Deep 不能自动替代 Normal。

### 3.2 脱敏真实回答和模拟盲评

- 6 条脱敏真实回答中，Deep 关键词覆盖较高，但引用支持 proxy 从 Normal 的 0.988 降至约 0.809；
- Deep P95 约 51.7 秒，Normal P95 约 11.8 秒；
- 规则化模拟盲评总分相同，Deep 的证据依据低 0.333；
- 模拟评分明确标记为 `simulated_human`，不是独立真人结果。

因此当前正确产品决策是：Deep 保留为用户显式模式，停止自动路由上线；不要为了指标增加 Agent 或复杂基础设施。

## 4. P5 重新评估：只做一个单缺陷实验

### 4.1 最高影响缺陷

V3 的运行时归因中，最明确的失败是：

- 有完整 EvidencePackage 时 Analyst 返回空 `claims`，导致后续生成只能回答“没有足够证据”；
- 少量样本在最终生成阶段漏掉已验证 Claim，属于第二优先级；
- 当前并没有证据证明需要新增 Reviewer、Supervisor 或新的路由 Agent。

所以 P5 只选择第一项：**Analyst 空 Claim/证据绑定失败**。这不是泛化 Prompt 调优，
而是一个可验证的结构化协议门禁。

### 4.2 本次实现边界

- EvidencePackage 有证据且 Analyst 没有 Claim 时，确定性 Reviewer 检查生成 `citation_gap`；
- 将原本可能被模型 Reviewer 判为 PASS 的状态改为现有 `REVISE` 路径；
- Revision 仍最多一次；修订后仍为空则安全降级；
- 不修改冻结数据、Judge 评分器、阈值、ACL 规则、主图或 Normal 路由；
- 新增一个最小回归测试，不添加新 Agent。

### 4.3 P5 保留门槛

只有同时满足以下条件才保留改动：

1. 空 Claim 失败样本得到修复，或至少不再被错误 PASS；
2. Claim F1 或目标类别指标稳定改善；
3. Faithfulness 下降不超过 0.02；
4. ACL 违规仍为 0；
5. 其他任务类别无明显回退；
6. 延迟、Token 和调用次数增量可解释。

若不满足，回滚 P5 改动并冻结当前架构。不能为了“让 P5 有结果”而放宽门槛、修改数据或继续堆 Prompt。

## 5. P6 评估：代码之外的独立校准与面试材料

P6 不应与 P5 混成继续改 Agent 的工作。当前 `data/深度研究V3人工校准评分.json` 仍等待真实评审者：

- 4 个冻结 case；
- 36 个确定性分层标签；
- 每条填写 `supported/not_supported` 和边界原因；
- 填写真实 `reviewer_id`；
- `independence_attestation=true`；
- 使用 `python -m tests.eval.summarize_v3_human_calibration --review-kind human` 汇总。

Codex 模拟结果只能作为开发诊断，不能写成真人盲评或面试中的人工实验结论。

在真人标签完成前，可以准备但不能冒充完成的三个面试案例：

1. 为什么普通问题走 Normal、复杂多证据问题才显式使用 Deep；
2. Reviewer 如何拦截不受支持 Claim，且 Revision 最多一次；
3. ACL 变化后历史 Evidence、Memory 和 Project Knowledge 如何 fail closed。

## 6. 后续唯一顺序

```text
P5 单缺陷回归与对照评测
  ↓
满足门槛则保留，否则冻结架构
  ↓
P6 独立真人校准（有实验室成员时）
  ↓
整理三条秋招案例和准确指标表述
  ↓
再评估单一外部证据适配器（可选）
```

近期明确不做：登录反复改造、更多 Agent、Supervisor、Redis、Neo4j、Qdrant、MinIO、
Plan-ReAct-MCP 主链替换、SFT/DPO、大规模 UI 重构和无失败归因依据的 Step-back。

## 7. 当前停止条件

当 P5 对照评测没有稳定收益，或真人校准尚未具备独立评审资源时，停止继续改代码，
保留现有架构和清晰的评测边界。此时项目已经足以作为“实验室科研知识协作 Agent”进行秋招展示，
重点讲清收益边界、质量/延迟取舍和安全闭环，而不是继续增加功能数量。
