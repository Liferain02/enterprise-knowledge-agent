 # Goal P1：完成实验室科研助手的可信项目知识发布闭环

  ## 目标

  将一次经过证据审查、ACL 校验和用户确认的科研结论，安全地沉淀为项目级知识记录，并支持后续追溯、撤销和替代。

  这个 Goal 只解决“知识如何可信地进入项目”的问题，不同时做知识图谱、复杂审核后台、自动知识合并或新的 Agent。

  ## 要解决的问题

  当前系统已经具备多轮问答、来源引用和 Mem0 个人记忆能力，但仍缺少一条完整的项目知识链路：

  Research Run
      ↓
  Claim
      ↓
  证据与 ACL 检查
      ↓
  用户独立发布
      ↓
  项目知识记录
      ↓
  查看来源、撤销或替代

  没有这条链路，系统只能“回答过”，不能形成实验室项目中可复用、可审计的知识资产。

  ## 本 Goal 的范围

  ### 1. 项目知识发布

  允许用户从 Research Run 中选择一个 Claim，主动执行“发布为项目知识”。

  发布动作必须独立于模型输出，模型不能自动把自己的回答写成正式项目知识。

  ### 2. 发布门禁

  只有以下条件全部满足时才能发布：

  - Claim 属于当前项目；
  - 当前用户拥有该项目的写入权限；
  - Research Run 的整体评测结果为 PASS；
  - Claim 存在有效来源；
  - 来源不能全部是 derived_only；
  - 来源经过 ACL 校验；
  - 不存在针对该 Claim 的明确负面 ReviewItem；
  - 发布请求具备幂等性；
  - 相同 Claim 不重复生成多条 active 知识。

  建议使用如下最小判断：

  可发布 =
  项目归属正确
  AND 用户有写权限
  AND 全局评测 PASS
  AND Claim 有来源
  AND 至少一个来源不是 derived_only
  AND ACL 校验通过
  AND 不存在负面审核项

  不能把“存在 ReviewerItem”简单等同于“必须存在 ReviewerItem”。没有人工 ReviewItem 的 PASS 结果仍然可以发布，但如果已经存在负面 ReviewItem，则必须拒绝。

  ### 3. 来源追溯

  每条项目知识必须能够追溯到：

  - 原始 Claim；
  - Research Run；
  - 来源文档；
  - 来源片段或引用位置；
  - 评测结果；
  - 发布用户；
  - 发布时间；
  - 当前状态。

  目标不是做复杂知识图谱，而是让面试者或实验室成员能够回答：

  > 这条知识是谁在什么情况下发布的，依据了什么材料，后来是否被撤销或替代？

  ### 4. 最小生命周期

  首版只保留三个状态：

  active       当前有效
  revoked      已撤销，不应继续作为有效知识使用
  superseded   已被新知识替代

  不引入复杂版本树。替代关系只需要记录：

  旧知识 -> 新知识

  发布新知识时，不自动修改旧知识，除非用户明确执行替代操作。

  ### 5. 最小 API 和页面能力

  服务端至少支持：

  POST   /api/projects/{project_id}/knowledge
  GET    /api/projects/{project_id}/knowledge
  GET    /api/projects/{project_id}/knowledge/{knowledge_id}
  POST   /api/projects/{project_id}/knowledge/{knowledge_id}/revoke
  POST   /api/projects/{project_id}/knowledge/{knowledge_id}/supersede

  Research Run 页面只需新增：

  - “发布为项目知识”按钮；
  - 发布前显示门禁结果；
  - 发布成功提示；
  - 已发布状态；
  - 进入知识详情的链接。

  不需要新建复杂管理后台。

  ## 实现步骤

  ### 第一步：完成现有代码审查

  重点确认当前实现是否真正覆盖：

  - 项目权限校验；
  - acl_verified=true 的强制要求；
  - 来源是否存在；
  - derived_only 来源拒绝；
  - 负面 ReviewItem 拒绝；
  - 重复发布幂等；
  - 撤销后不能继续作为 active 知识；
  - 替代关系是否完整记录。

  ### 第二步：补齐服务端测试

  至少覆盖以下场景：

   场景                    预期
  ━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   正常 PASS Claim 发布    成功
  ──────────────────────  ──────────────────────────────────
   非 PASS Claim 发布      拒绝
  ──────────────────────  ──────────────────────────────────
   无来源 Claim 发布       拒绝
  ──────────────────────  ──────────────────────────────────
   只有 Derived 来源       拒绝
  ──────────────────────  ──────────────────────────────────
   ACL 未验证              拒绝
  ──────────────────────  ──────────────────────────────────
   存在负面 ReviewItem     拒绝
  ──────────────────────  ──────────────────────────────────
   无项目写权限            拒绝
  ──────────────────────  ──────────────────────────────────
   重复发布相同 Claim      幂等，不重复创建
  ──────────────────────  ──────────────────────────────────
   撤销 active 知识        状态变为 revoked
  ──────────────────────  ──────────────────────────────────
   撤销后再次撤销          幂等
  ──────────────────────  ──────────────────────────────────
   替代旧知识              旧记录 superseded，新记录 active
  ──────────────────────  ──────────────────────────────────
   越权查看其他项目知识    拒绝

  ### 第三步：补齐最小页面交互

  页面只负责展示和触发，不在前端复制门禁逻辑。

  前端应以服务端返回的门禁结果为准，例如：

  {
    "publishable": false,
    "reasons": [
      "ACL 尚未完成验证"
    ]
  }

  前端显示失败原因，但不能自行判断“看起来可以发布”。

  ### 第四步：真实验收

  使用浏览器完成一条完整链路：

  1. 登录；
  2. 进入一个已有 Research Run；
  3. 查看 Claim 和来源；
  4. 发布为项目知识；
  5. 打开项目知识列表；
  6. 查看详情和来源；
  7. 撤销知识；
  8. 确认状态变为 revoked；
  9. 再次打开列表，确认不会显示为 active；
  10. 测试重复点击和刷新后的状态一致性。

  ### 第五步：提交阶段性结果

  完成标准满足后执行：

  git add .
  git commit -m "feat: 完成可信项目知识发布闭环"

  ## 明确不在本 Goal 内

  以下内容暂时不做：

  - 自动把所有回答沉淀为知识；
  - 自动合并相似 Claim；
  - 知识图谱；
  - 复杂审核工作流；
  - 多级审批角色；
  - 新增 Agent；
  - 自动改变路由策略；
  - 把项目知识立即接入普通检索；
  - 让模型根据自己生成的知识再次证明自己；
  - Redis 或新的基础设施；
  - 生产部署、集群化和高并发优化。

  尤其是“发布后的知识接入检索”应该放在下一个 Goal，因为必须先单独验证它不会造成自我强化：

  模型生成 Claim
      ↓
  模型自己发布
      ↓
  检索到自己发布的 Claim
      ↓
  再次把它当作证据

  这条链路在没有隔离实验和来源标记前不能直接启用。

  ## 完成验收标准

  这个 Goal 完成时必须同时满足：

  - 发布门禁有服务端测试；
  - 越权和 ACL 失败路径有测试；
  - 重复发布不会产生重复 active 记录；
  - 撤销和替代状态可以真实操作；
  - 每条知识可以追溯到 Claim、Run、来源和发布者；
  - Research Run 页面可以完成发布；
  - 浏览器真实验收通过；
  - 全量测试通过；
  - 没有引入新的 Agent 或复杂基础设施；
  - 已执行 git add . 和 git commit。

  ## 后续顺序

  完成 P1 后再按以下顺序推进：

  P1 可信项目知识发布闭环
    ↓
  P2 项目知识 Wiki 与详情展示
    ↓
  P3 知识接入检索，并验证不自我强化
    ↓
  P4 Selective Routing
    ↓
  P5 基于评测结果优化 Agent 和提示词
    ↓
  P6 真人评分与秋招展示材料

  因此，当前不应该继续扩展多 Agent，而应该先把 P1 做到“可发布、可追溯、可撤销、可测试、可提交”。